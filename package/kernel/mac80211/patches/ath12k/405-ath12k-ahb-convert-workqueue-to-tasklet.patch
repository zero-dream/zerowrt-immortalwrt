From: JiaY-shi <shi05275@163.com>
Subject: [PATCH] wifi: ath12k: ahb: convert CE interrupt from workqueue to tasklet

Patch 200 reverts the workqueue-to-tasklet conversion for pci.c.  Apply
the same to ahb.c so ATH12K_AHB builds against the backported ce.h.

--- a/drivers/net/wireless/ath/ath12k/ahb.c
+++ b/drivers/net/wireless/ath/ath12k/ahb.c
@@ -142,7 +142,7 @@ static void ath12k_ahb_write32(struct at
 		iowrite32(value, ab->mem + offset);
 }
 
-static void ath12k_ahb_cancel_workqueue(struct ath12k_base *ab)
+static void ath12k_ahb_cancel_tasklet(struct ath12k_base *ab)
 {
 	int i;
 
@@ -152,7 +152,7 @@ static void ath12k_ahb_cancel_workqueue(
 		if (ath12k_ce_get_attr_flags(ab, i) & CE_ATTR_DIS_INTR)
 			continue;
 
-		cancel_work_sync(&ce_pipe->intr_wq);
+		tasklet_kill(&ce_pipe->intr_tq);
 	}
 }
 
@@ -331,7 +331,7 @@ static void ath12k_ahb_stop(struct ath12
 	if (!test_bit(ATH12K_FLAG_CRASH_FLUSH, &ab->dev_flags))
 		ath12k_ahb_ce_irqs_disable(ab);
 	ath12k_ahb_sync_ce_irqs(ab);
-	ath12k_ahb_cancel_workqueue(ab);
+	ath12k_ahb_cancel_tasklet(ab);
 	timer_delete_sync(&ab->rx_replenish_retry);
 	ath12k_ce_cleanup_pipes(ab);
 }
@@ -602,9 +602,9 @@ static void ath12k_ahb_init_qmi_ce_confi
 	ab->qmi.service_ins_id = ab->hw_params->qmi_service_ins_id;
 }
 
-static void ath12k_ahb_ce_workqueue(struct work_struct *work)
+static void ath12k_ahb_ce_tasklet(struct tasklet_struct *t)
 {
-	struct ath12k_ce_pipe *ce_pipe = from_work(ce_pipe, work, intr_wq);
+	struct ath12k_ce_pipe *ce_pipe = from_tasklet(ce_pipe, t, intr_tq);
 
 	ath12k_ce_per_engine_service(ce_pipe->ab, ce_pipe->pipe_num);
 
@@ -620,7 +620,7 @@ static irqreturn_t ath12k_ahb_ce_interru
 
 	ath12k_ahb_ce_irq_disable(ce_pipe->ab, ce_pipe->pipe_num);
 
-	queue_work(system_bh_wq, &ce_pipe->intr_wq);
+	tasklet_schedule(&ce_pipe->intr_tq);
 
 	return IRQ_HANDLED;
 }
@@ -753,7 +753,7 @@ static int ath12k_ahb_config_irq(struct
 
 		irq_idx = ATH12K_IRQ_CE0_OFFSET + i;
 
-		INIT_WORK(&ce_pipe->intr_wq, ath12k_ahb_ce_workqueue);
+		tasklet_setup(&ce_pipe->intr_tq, ath12k_ahb_ce_tasklet);
 		irq = platform_get_irq_byname(ab->pdev, irq_name[irq_idx]);
 		ret = devm_request_irq(ab->dev, irq, ath12k_ahb_ce_interrupt_handler,
 				       IRQF_TRIGGER_RISING, irq_name[irq_idx],
