--- a/drivers/net/ethernet/qualcomm/ppe/ppe_port.c
+++ b/drivers/net/ethernet/qualcomm/ppe/ppe_port.c
@@ -1391,6 +1391,10 @@ int ppe_port_mac_init(struct ppe_device
 		ppe_ports->port[i].interface = phy_mode;
 		ppe_ports->port[i].mac_type = PPE_MAC_TYPE_GMAC;
 
+		dev_info(ppe_dev->dev,
+			 "PPE debug: preparing port id=%d node=%s mode=%s\n",
+			 port, port_node->name, phy_modes(phy_mode));
+
 		ret = ppe_port_clock_init(&ppe_ports->port[i]);
 		if (ret) {
 			dev_err(ppe_dev->dev, "Failed to initialize port clocks\n");
@@ -1410,6 +1414,8 @@ int ppe_port_mac_init(struct ppe_device
 			goto err_port_setup;
 		}
 
+		dev_info(ppe_dev->dev, "PPE debug: registered port id=%d\n", port);
+
 		i++;
 	}
 
