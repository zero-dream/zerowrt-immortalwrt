From 6c3eb359715e272fbdc62bf9ecfb8f82a1baac7a Mon Sep 17 00:00:00 2001
From: ViKing <vking@example.com>
Date: Mon, 31 Aug 2026 23:20:00 +0800
Subject: [PATCH] net: ethernet: qualcomm: ppe: fix EDMA resource lifecycle

Allocate EDMA error statistics before enabling interrupts. Keep them alive
until IRQ teardown. Track every successful IRQ request and enabled clock so
partial setup failures and driver removal release only live resources.

Preserve signed IRQ lookup errors and validate child nodes and reset
operations. Centralize ring and hardware cleanup. Also disable the selected
per-port TX rings instead of accidentally disabling the first CPU rings.

Track enabled NAPI separately from registration and quiesce shared RX before
unregistering ports and freeing statistics. Unmap queued RX buffers using
the original mapping type and size, free partial assembly, and bound partial
ring allocation unwind with a signed index.

Respect napi_complete_done() before rearming and serialize completion, IRQ
scheduling and interrupt enable/disable under a per-ring lock. Stop cannot
be undone by the polling tail.

Signed-off-by: ViKing <vking@example.com>
---
--- a/drivers/net/ethernet/qualcomm/ppe/edma.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma.c
@@ -12,7 +12,6 @@
 #include <linux/module.h>
 #include <linux/netdevice.h>
 #include <linux/of_irq.h>
-#include <linux/platform_device.h>
 #include <linux/printk.h>
 #include <linux/regmap.h>
 #include <linux/reset.h>
@@ -166,41 +165,55 @@ static struct edma_hw_info ipq9574_hw_in
 };
 
 static int edma_clock_set_and_enable(struct device *dev,
-				     const char *id, unsigned long rate)
+				     const char *id, unsigned long rate,
+				     struct clk **enabled_clk)
 {
 	struct device_node *edma_np;
 	struct clk *clk = NULL;
 	int ret;
 
 	edma_np = of_get_child_by_name(dev->of_node, EDMA_NODE_NAME);
+	if (!edma_np)
+		return -ENODEV;
 
 	clk = devm_get_clk_from_child(dev, edma_np, id);
+	of_node_put(edma_np);
 	if (IS_ERR(clk)) {
 		dev_err(dev, "clk %s get failed\n", id);
-		of_node_put(edma_np);
 		return PTR_ERR(clk);
 	}
 
 	ret = clk_set_rate(clk, rate);
 	if (ret) {
 		dev_err(dev, "set %lu rate for %s failed\n", rate, id);
-		of_node_put(edma_np);
 		return ret;
 	}
 
 	ret = clk_prepare_enable(clk);
 	if (ret) {
 		dev_err(dev, "clk %s enable failed\n", id);
-		of_node_put(edma_np);
 		return ret;
 	}
 
-	of_node_put(edma_np);
+	*enabled_clk = clk;
 	dev_dbg(dev, "set %lu rate for %s\n", rate, id);
 
 	return 0;
 }
 
+static void edma_clock_disable(void)
+{
+	if (edma_ctx->apb_clk) {
+		clk_disable_unprepare(edma_ctx->apb_clk);
+		edma_ctx->apb_clk = NULL;
+	}
+
+	if (edma_ctx->sys_clk) {
+		clk_disable_unprepare(edma_ctx->sys_clk);
+		edma_ctx->sys_clk = NULL;
+	}
+}
+
 static int edma_clock_configure(void)
 {
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
@@ -211,14 +224,16 @@ static int edma_clock_configure(void)
 	ppe_rate = ppe_dev->clk_rate;
 
 	ret = edma_clock_set_and_enable(dev, clock_name[EDMA_SYS_CLK],
-					ppe_rate);
+					ppe_rate, &edma_ctx->sys_clk);
 	if (ret)
 		return ret;
 
 	ret = edma_clock_set_and_enable(dev, clock_name[EDMA_APB_CLK],
-					ppe_rate);
-	if (ret)
+					ppe_rate, &edma_ctx->apb_clk);
+	if (ret) {
+		edma_clock_disable();
 		return ret;
+	}
 
 	dev_dbg(dev, "EDMA clocks are configured\n");
 
@@ -440,6 +455,54 @@ static irqreturn_t edma_misc_handle_irq(
 	return IRQ_HANDLED;
 }
 
+static void edma_irq_names_free(void)
+{
+	struct edma_hw_info *hw_info = edma_ctx->hw_info;
+	u32 i;
+
+	if (edma_rxdesc_irq_name) {
+		for (i = 0; i < hw_info->rx->num_rings; i++)
+			kfree(edma_rxdesc_irq_name[i]);
+		kfree(edma_rxdesc_irq_name);
+		edma_rxdesc_irq_name = NULL;
+	}
+
+	if (edma_txcmpl_irq_name) {
+		for (i = 0; i < hw_info->txcmpl->num_rings; i++)
+			kfree(edma_txcmpl_irq_name[i]);
+		kfree(edma_txcmpl_irq_name);
+		edma_txcmpl_irq_name = NULL;
+	}
+}
+
+static void edma_irq_unregister(void)
+{
+	struct device *dev = edma_ctx->ppe_dev->dev;
+	u32 i;
+
+	if (edma_ctx->misc_irq_registered) {
+		synchronize_irq(edma_ctx->intr_info.intr_misc);
+		free_irq(edma_ctx->intr_info.intr_misc, dev);
+		edma_ctx->misc_irq_registered = false;
+	}
+
+	while (edma_ctx->rxdesc_irqs_registered) {
+		i = --edma_ctx->rxdesc_irqs_registered;
+		synchronize_irq(edma_ctx->intr_info.intr_rx[i]);
+		free_irq(edma_ctx->intr_info.intr_rx[i],
+			 &edma_ctx->rx_rings[i]);
+	}
+
+	while (edma_ctx->txcmpl_irqs_registered) {
+		i = --edma_ctx->txcmpl_irqs_registered;
+		synchronize_irq(edma_ctx->intr_info.intr_txcmpl[i]);
+		free_irq(edma_ctx->intr_info.intr_txcmpl[i],
+			 &edma_ctx->txcmpl_rings[i]);
+	}
+
+	edma_irq_names_free();
+}
+
 static int edma_irq_register(void)
 {
 	struct edma_hw_info *hw_info = edma_ctx->hw_info;
@@ -451,16 +514,16 @@ static int edma_irq_register(void)
 	u32 i;
 
 	/* Request IRQ for TXCMPL rings. */
-	edma_txcmpl_irq_name = kzalloc((sizeof(char *) * txcmpl->num_rings), GFP_KERNEL);
+	edma_txcmpl_irq_name =
+		kcalloc(txcmpl->num_rings, sizeof(char *), GFP_KERNEL);
 	if (!edma_txcmpl_irq_name)
 		return -ENOMEM;
 
 	for (i = 0; i < txcmpl->num_rings; i++) {
-		edma_txcmpl_irq_name[i] = kzalloc((sizeof(char *) * EDMA_IRQ_NAME_SIZE),
-						  GFP_KERNEL);
+		edma_txcmpl_irq_name[i] = kzalloc(EDMA_IRQ_NAME_SIZE, GFP_KERNEL);
 		if (!edma_txcmpl_irq_name[i]) {
 			ret = -ENOMEM;
-			goto txcmpl_ring_irq_name_alloc_fail;
+			goto irq_register_failed;
 		}
 
 		snprintf(edma_txcmpl_irq_name[i], EDMA_IRQ_NAME_SIZE, "txcmpl_%d",
@@ -475,8 +538,9 @@ static int edma_irq_register(void)
 		if (ret) {
 			pr_err("TXCMPL ring IRQ:%d request %d failed\n",
 			       edma_ctx->intr_info.intr_txcmpl[i], i);
-			goto txcmpl_ring_intr_req_fail;
+			goto irq_register_failed;
 		}
+		edma_ctx->txcmpl_irqs_registered++;
 
 		pr_debug("TXCMPL ring: %d IRQ:%d request success: %s\n",
 			 txcmpl->ring_start + i,
@@ -485,22 +549,21 @@ static int edma_irq_register(void)
 	}
 
 	/* Request IRQ for RXDESC rings. */
-	edma_rxdesc_irq_name = kzalloc((sizeof(char *) * rx->num_rings),
-				       GFP_KERNEL);
+	edma_rxdesc_irq_name =
+		kcalloc(rx->num_rings, sizeof(char *), GFP_KERNEL);
 	if (!edma_rxdesc_irq_name) {
 		ret = -ENOMEM;
-		goto rxdesc_irq_name_alloc_fail;
+		goto irq_register_failed;
 	}
 
 	for (i = 0; i < rx->num_rings; i++) {
-		edma_rxdesc_irq_name[i] = kzalloc((sizeof(char *) * EDMA_IRQ_NAME_SIZE),
-						  GFP_KERNEL);
+		edma_rxdesc_irq_name[i] = kzalloc(EDMA_IRQ_NAME_SIZE, GFP_KERNEL);
 		if (!edma_rxdesc_irq_name[i]) {
 			ret = -ENOMEM;
-			goto rxdesc_ring_irq_name_alloc_fail;
+			goto irq_register_failed;
 		}
 
-		snprintf(edma_rxdesc_irq_name[i], 20, "rxdesc_%d",
+		snprintf(edma_rxdesc_irq_name[i], EDMA_IRQ_NAME_SIZE, "rxdesc_%d",
 			 rx->ring_start + i);
 
 		irq_set_status_flags(edma_ctx->intr_info.intr_rx[i], IRQ_DISABLE_UNLAZY);
@@ -512,8 +575,9 @@ static int edma_irq_register(void)
 		if (ret) {
 			pr_err("RXDESC ring IRQ:%d request failed\n",
 			       edma_ctx->intr_info.intr_rx[i]);
-			goto rx_desc_ring_intr_req_fail;
+			goto irq_register_failed;
 		}
+		edma_ctx->rxdesc_irqs_registered++;
 
 		pr_debug("RXDESC ring: %d IRQ:%d request success: %s\n",
 			 rx->ring_start + i,
@@ -528,35 +592,14 @@ static int edma_irq_register(void)
 	if (ret) {
 		pr_err("MISC IRQ:%d request failed\n",
 		       edma_ctx->intr_info.intr_misc);
-		goto misc_intr_req_fail;
+		goto irq_register_failed;
 	}
+	edma_ctx->misc_irq_registered = true;
 
 	return 0;
 
-misc_intr_req_fail:
-	/* Free IRQ for RXDESC rings */
-	for (i = 0; i < rx->num_rings; i++) {
-		synchronize_irq(edma_ctx->intr_info.intr_rx[i]);
-		free_irq(edma_ctx->intr_info.intr_rx[i],
-			 (void *)&edma_ctx->rx_rings[i]);
-	}
-rx_desc_ring_intr_req_fail:
-	for (i = 0; i < rx->num_rings; i++)
-		kfree(edma_rxdesc_irq_name[i]);
-rxdesc_ring_irq_name_alloc_fail:
-	kfree(edma_rxdesc_irq_name);
-rxdesc_irq_name_alloc_fail:
-	for (i = 0; i < txcmpl->num_rings; i++) {
-		synchronize_irq(edma_ctx->intr_info.intr_txcmpl[i]);
-		free_irq(edma_ctx->intr_info.intr_txcmpl[i],
-			 (void *)&edma_ctx->txcmpl_rings[i]);
-	}
-txcmpl_ring_intr_req_fail:
-	for (i = 0; i < txcmpl->num_rings; i++)
-		kfree(edma_txcmpl_irq_name[i]);
-txcmpl_ring_irq_name_alloc_fail:
-	kfree(edma_txcmpl_irq_name);
-
+irq_register_failed:
+	edma_irq_unregister();
 	return ret;
 }
 
@@ -568,88 +611,95 @@ static int edma_irq_configure(void)
 	struct edma_ring_info *rx = hw_info->rx;
 	char edma_irq_name[EDMA_IRQ_NAME_SIZE];
 	struct device *dev = ppe_dev->dev;
-	struct platform_device *pdev;
 	struct device_node *edma_np;
+	int ret;
 	u32 i;
 
-	pdev = to_platform_device(dev);
 	edma_np = of_get_child_by_name(dev->of_node, EDMA_NODE_NAME);
+	if (!edma_np)
+		return -ENODEV;
+
 	edma_ctx->intr_info.intr_txcmpl = kzalloc((sizeof(*edma_ctx->intr_info.intr_txcmpl) *
 						  txcmpl->num_rings), GFP_KERNEL);
 	if (!edma_ctx->intr_info.intr_txcmpl) {
-		of_node_put(edma_np);
-		return -ENOMEM;
+		ret = -ENOMEM;
+		goto irq_configure_failed;
 	}
 
 	/* Get TXCMPL rings IRQ numbers. */
 	for (i = 0; i < txcmpl->num_rings; i++) {
 		snprintf(edma_irq_name, sizeof(edma_irq_name), "txcmpl_%d",
 			 txcmpl->ring_start + i);
-		edma_ctx->intr_info.intr_txcmpl[i] = of_irq_get_byname(edma_np, edma_irq_name);
-		if (edma_ctx->intr_info.intr_txcmpl[i] < 0) {
+		ret = of_irq_get_byname(edma_np, edma_irq_name);
+		if (ret < 0) {
 			dev_err(dev, "%s: txcmpl_info.intr[%u] irq get failed\n",
 				edma_np->name, i);
-			of_node_put(edma_np);
-			kfree(edma_ctx->intr_info.intr_txcmpl);
-			return edma_ctx->intr_info.intr_txcmpl[i];
+			goto irq_configure_failed;
 		}
+		edma_ctx->intr_info.intr_txcmpl[i] = ret;
 
-		dev_dbg(dev, "%s: intr_info.intr_txcmpl[%u] = %u\n",
+		dev_dbg(dev, "%s: intr_info.intr_txcmpl[%u] = %d\n",
 			edma_np->name, i, edma_ctx->intr_info.intr_txcmpl[i]);
 	}
 
 	edma_ctx->intr_info.intr_rx = kzalloc((sizeof(*edma_ctx->intr_info.intr_rx) *
 					      rx->num_rings), GFP_KERNEL);
 	if (!edma_ctx->intr_info.intr_rx) {
-		of_node_put(edma_np);
-		kfree(edma_ctx->intr_info.intr_txcmpl);
-		return -ENOMEM;
+		ret = -ENOMEM;
+		goto irq_configure_failed;
 	}
 
 	/* Get RXDESC rings IRQ numbers. */
 	for (i = 0; i < rx->num_rings; i++) {
 		snprintf(edma_irq_name, sizeof(edma_irq_name), "rxdesc_%d",
 			 rx->ring_start + i);
-		edma_ctx->intr_info.intr_rx[i] = of_irq_get_byname(edma_np, edma_irq_name);
-		if (edma_ctx->intr_info.intr_rx[i] < 0) {
+		ret = of_irq_get_byname(edma_np, edma_irq_name);
+		if (ret < 0) {
 			dev_err(dev, "%s: rx_queue_map_info.intr[%u] irq get failed\n",
 				edma_np->name, i);
-			of_node_put(edma_np);
-			kfree(edma_ctx->intr_info.intr_rx);
-			kfree(edma_ctx->intr_info.intr_txcmpl);
-			return edma_ctx->intr_info.intr_rx[i];
+			goto irq_configure_failed;
 		}
+		edma_ctx->intr_info.intr_rx[i] = ret;
 
-		dev_dbg(dev, "%s: intr_info.intr_rx[%u] = %u\n",
+		dev_dbg(dev, "%s: intr_info.intr_rx[%u] = %d\n",
 			edma_np->name, i, edma_ctx->intr_info.intr_rx[i]);
 	}
 
 	/* Get misc IRQ number. */
-	edma_ctx->intr_info.intr_misc = of_irq_get_byname(edma_np, "misc");
-	if (edma_ctx->intr_info.intr_misc < 0) {
+	ret = of_irq_get_byname(edma_np, "misc");
+	if (ret < 0) {
 		dev_err(dev, "%s: misc_intr irq get failed\n", edma_np->name);
-		of_node_put(edma_np);
-		kfree(edma_ctx->intr_info.intr_rx);
-		kfree(edma_ctx->intr_info.intr_txcmpl);
-		return edma_ctx->intr_info.intr_misc;
+		goto irq_configure_failed;
 	}
+	edma_ctx->intr_info.intr_misc = ret;
 
-	of_node_put(edma_np);
-
-	dev_dbg(dev, "%s: misc IRQ:%u\n", edma_np->name,
+	dev_dbg(dev, "%s: misc IRQ:%d\n", edma_np->name,
 		edma_ctx->intr_info.intr_misc);
+	of_node_put(edma_np);
 
 	return 0;
+
+irq_configure_failed:
+	of_node_put(edma_np);
+	kfree(edma_ctx->intr_info.intr_rx);
+	edma_ctx->intr_info.intr_rx = NULL;
+	kfree(edma_ctx->intr_info.intr_txcmpl);
+	edma_ctx->intr_info.intr_txcmpl = NULL;
+	return ret;
 }
 
 static int edma_alloc_rings(void)
 {
-	if (edma_cfg_tx_rings_alloc()) {
+	int ret;
+
+	ret = edma_cfg_tx_rings_alloc();
+	if (ret) {
 		pr_err("Error in allocating Tx rings\n");
-		return -ENOMEM;
+		return ret;
 	}
 
-	if (edma_cfg_rx_rings_alloc()) {
+	ret = edma_cfg_rx_rings_alloc();
+	if (ret) {
 		pr_err("Error in allocating Rx rings\n");
 		goto rx_rings_alloc_fail;
 	}
@@ -659,7 +709,7 @@ static int edma_alloc_rings(void)
 rx_rings_alloc_fail:
 	edma_cfg_tx_rings_cleanup();
 
-	return -ENOMEM;
+	return ret;
 }
 
 static int edma_hw_reset(void)
@@ -668,19 +718,26 @@ static int edma_hw_reset(void)
 	struct device *dev = ppe_dev->dev;
 	struct reset_control *edma_hw_rst;
 	struct device_node *edma_np;
+	int ret;
 
 	edma_np = of_get_child_by_name(dev->of_node, EDMA_NODE_NAME);
+	if (!edma_np)
+		return -ENODEV;
+
 	edma_hw_rst = of_reset_control_array_get_exclusive(edma_np);
 	if (IS_ERR(edma_hw_rst)) {
-		of_node_put(edma_np);
-		return PTR_ERR(edma_hw_rst);
+		ret = PTR_ERR(edma_hw_rst);
+		goto reset_failed;
 	}
 
-	/* 100ms delay is required by hardware to reset EDMA. */
-	reset_control_assert(edma_hw_rst);
+	ret = reset_control_assert(edma_hw_rst);
+	if (ret)
+		goto reset_control_failed;
 	fsleep(100);
 
-	reset_control_deassert(edma_hw_rst);
+	ret = reset_control_deassert(edma_hw_rst);
+	if (ret)
+		goto reset_control_failed;
 	fsleep(100);
 
 	reset_control_put(edma_hw_rst);
@@ -689,6 +746,12 @@ static int edma_hw_reset(void)
 	of_node_put(edma_np);
 
 	return 0;
+
+reset_control_failed:
+	reset_control_put(edma_hw_rst);
+reset_failed:
+	of_node_put(edma_np);
+	return ret;
 }
 
 static int edma_hw_configure(void)
@@ -716,7 +779,7 @@ static int edma_hw_configure(void)
 	}
 
 	/* Allocate memory for netdevices. */
-	edma_ctx->netdev_arr = kzalloc((sizeof(**edma_ctx->netdev_arr) *
+	edma_ctx->netdev_arr = kzalloc((sizeof(*edma_ctx->netdev_arr) *
 					 hw_info->max_ports),
 					 GFP_KERNEL);
 	if (!edma_ctx->netdev_arr)
@@ -775,13 +838,13 @@ static int edma_hw_configure(void)
 	reg = EDMA_BASE_OFFSET + EDMA_REG_DMAR_CTRL_ADDR;
 	ret = regmap_write(regmap, reg, data);
 	if (ret)
-		return ret;
+		goto edma_cfg_rx_rings_failed;
 
 	data = EDMA_TX_TIMEOUT_THRESH_VAL;
 	reg = EDMA_BASE_OFFSET + EDMA_REG_TX_TIMEOUT_THRESH_ADDR;
 	ret = regmap_write(regmap, reg, data);
 	if (ret)
-		return ret;
+		goto edma_cfg_rx_rings_failed;
 
 	/* Set Miscellaneous error interrupt mask. */
 	data = EDMA_MISC_AXI_RD_ERR_MASK |
@@ -805,7 +868,7 @@ static int edma_hw_configure(void)
 	reg = EDMA_BASE_OFFSET + EDMA_REG_PORT_CTRL_ADDR;
 	ret = regmap_write(regmap, reg, data);
 	if (ret)
-		return ret;
+		goto edma_port_enable_failed;
 
 	ret = (int)edma_configure_ucast_prio_map_tbl();
 	if (ret) {
@@ -821,24 +884,64 @@ static int edma_hw_configure(void)
 		goto edma_cfg_rx_rps_hash_map_failed;
 	}
 
+	edma_ctx->hw_configured = true;
 	return 0;
 
 edma_cfg_rx_rps_hash_map_failed:
 configure_ucast_prio_map_tbl_failed:
-	edma_cfg_rx_napi_disable();
-	edma_cfg_rx_napi_delete();
+	regmap_write(regmap, EDMA_BASE_OFFSET + EDMA_REG_PORT_CTRL_ADDR, 0);
+edma_port_enable_failed:
 	edma_cfg_rx_rings_disable();
+	edma_cfg_rx_napi_delete();
 edma_cfg_rx_rings_failed:
 	edma_cfg_tx_rings_cleanup();
 	edma_cfg_rx_rings_cleanup();
 edma_alloc_rings_failed:
 	free_netdev(edma_ctx->dummy_dev);
+	edma_ctx->dummy_dev = NULL;
 dummy_dev_alloc_failed:
 	kfree(edma_ctx->netdev_arr);
+	edma_ctx->netdev_arr = NULL;
 
 	return ret;
 }
 
+/* Stop shared RX before any port netdev or per-port statistics are freed.
+ * Port teardown still owns TX NAPI, which ndo_stop disables exactly once.
+ */
+void edma_quiesce(void)
+{
+	struct regmap *regmap = edma_ctx->ppe_dev->regmap;
+	u32 i;
+
+	if (!edma_ctx->hw_configured)
+		return;
+
+	regmap_write(regmap, EDMA_BASE_OFFSET + EDMA_REG_PORT_CTRL_ADDR, 0);
+	for (i = 1; i <= edma_ctx->hw_info->max_ports; i++)
+		edma_cfg_tx_disable_interrupts(i);
+	edma_cfg_rx_disable_interrupts();
+	edma_disable_misc_interrupt();
+	edma_cfg_rx_rings_disable();
+	edma_cfg_rx_napi_disable();
+}
+
+static void edma_hw_destroy(void)
+{
+	if (!edma_ctx->hw_configured)
+		return;
+
+	edma_quiesce();
+	edma_cfg_rx_napi_delete();
+	edma_cfg_rx_rings_cleanup();
+	edma_cfg_tx_rings_cleanup();
+	free_netdev(edma_ctx->dummy_dev);
+	edma_ctx->dummy_dev = NULL;
+	kfree(edma_ctx->netdev_arr);
+	edma_ctx->netdev_arr = NULL;
+	edma_ctx->hw_configured = false;
+}
+
 /**
  * edma_destroy - EDMA Destroy.
  * @ppe_dev: PPE device
@@ -847,56 +950,35 @@ dummy_dev_alloc_failed:
  */
 void edma_destroy(struct ppe_device *ppe_dev)
 {
-	struct edma_hw_info *hw_info = edma_ctx->hw_info;
-	struct edma_ring_info *txcmpl = hw_info->txcmpl;
-	struct edma_ring_info *rx = hw_info->rx;
+	struct edma_hw_info *hw_info;
 	u32 i;
 
+	if (WARN_ON_ONCE(!edma_ctx || edma_ctx->ppe_dev != ppe_dev))
+		return;
+
+	hw_info = edma_ctx->hw_info;
+
 	if (edma_ctx->rx_rps_ctl_table_hdr) {
 		unregister_sysctl_table(edma_ctx->rx_rps_ctl_table_hdr);
 		edma_ctx->rx_rps_ctl_table_hdr = NULL;
 	}
 
-	for (i = 1; i <= hw_info->max_ports; i++)
-		edma_cfg_tx_disable_interrupts(i);
-
-	edma_cfg_rx_disable_interrupts();
-	edma_disable_misc_interrupt();
-
-	/* Free IRQ for Tx cmpl rings. */
-	for (i = 0; i < txcmpl->num_rings; i++) {
-		synchronize_irq(edma_ctx->intr_info.intr_txcmpl[i]);
-
-		free_irq(edma_ctx->intr_info.intr_txcmpl[i],
-			 (void *)&edma_ctx->txcmpl_rings[i]);
-		kfree(edma_txcmpl_irq_name[i]);
-	}
-	kfree(edma_txcmpl_irq_name);
+	if (edma_ctx->hw_configured) {
+		for (i = 1; i <= hw_info->max_ports; i++)
+			edma_cfg_tx_disable_interrupts(i);
 
-	/* Free IRQ for Rx DESC rings */
-	for (i = 0; i < rx->num_rings; i++) {
-		synchronize_irq(edma_ctx->intr_info.intr_rx[i]);
-		free_irq(edma_ctx->intr_info.intr_rx[i],
-			 (void *)&edma_ctx->rx_rings[i]);
-		kfree(edma_rxdesc_irq_name[i]);
+		edma_cfg_rx_disable_interrupts();
+		edma_disable_misc_interrupt();
 	}
-	kfree(edma_rxdesc_irq_name);
-
-	/* Free Misc IRQ */
-	synchronize_irq(edma_ctx->intr_info.intr_misc);
-	free_irq(edma_ctx->intr_info.intr_misc, (void *)(ppe_dev->dev));
 
+	edma_irq_unregister();
 	kfree(edma_ctx->intr_info.intr_rx);
+	edma_ctx->intr_info.intr_rx = NULL;
 	kfree(edma_ctx->intr_info.intr_txcmpl);
-
-	edma_cfg_rx_napi_disable();
-	edma_cfg_rx_napi_delete();
-	edma_cfg_rx_rings_disable();
-	edma_cfg_rx_rings_cleanup();
-	edma_cfg_tx_rings_cleanup();
-
-	free_netdev(edma_ctx->dummy_dev);
-	kfree(edma_ctx->netdev_arr);
+	edma_ctx->intr_info.intr_txcmpl = NULL;
+	edma_hw_destroy();
+	edma_err_stats_free();
+	edma_clock_disable();
 }
 
 /* EDMA Rx RPS core sysctl table */
@@ -948,30 +1030,34 @@ int edma_setup(struct ppe_device *ppe_de
 		return -EINVAL;
 	}
 
+	ret = edma_err_stats_alloc();
+	if (ret) {
+		dev_err(dev, "Unable to allocate EDMA per-CPU stats\n");
+		goto stats_alloc_failed;
+	}
+
 	ret = edma_clock_configure();
 	if (ret) {
 		dev_err(dev, "Error in configuring the EDMA clocks\n");
-		return ret;
+		goto clock_configure_failed;
 	}
 
 	ret = edma_hw_configure();
 	if (ret) {
 		dev_err(dev, "Error in edma configuration\n");
-		return ret;
+		goto hw_configure_failed;
 	}
 
 	ret = edma_irq_configure();
 	if (ret) {
 		dev_err(dev, "Error in irq initialization\n");
-		return ret;
+		goto irq_configure_failed;
 	}
 
 	ret = edma_irq_register();
 	if (ret) {
 		dev_err(dev, "Error in irq registration\n");
-		kfree(edma_ctx->intr_info.intr_rx);
-		kfree(edma_ctx->intr_info.intr_txcmpl);
-		return ret;
+		goto irq_register_failed;
 	}
 
 	edma_cfg_rx_enable_interrupts();
@@ -980,4 +1066,20 @@ int edma_setup(struct ppe_device *ppe_de
 	dev_info(dev, "EDMA configuration successful\n");
 
 	return 0;
+
+irq_register_failed:
+irq_configure_failed:
+	kfree(edma_ctx->intr_info.intr_rx);
+	edma_ctx->intr_info.intr_rx = NULL;
+	kfree(edma_ctx->intr_info.intr_txcmpl);
+	edma_ctx->intr_info.intr_txcmpl = NULL;
+	edma_hw_destroy();
+hw_configure_failed:
+	edma_clock_disable();
+clock_configure_failed:
+	edma_err_stats_free();
+stats_alloc_failed:
+	unregister_sysctl_table(edma_ctx->rx_rps_ctl_table_hdr);
+	edma_ctx->rx_rps_ctl_table_hdr = NULL;
+	return ret;
 }
--- a/drivers/net/ethernet/qualcomm/ppe/edma.h
+++ b/drivers/net/ethernet/qualcomm/ppe/edma.h
@@ -103,11 +103,11 @@ struct edma_hw_info {
  */
 struct edma_intr_info {
 	u32 intr_mask_rx;
-	u32 *intr_rx;
+	int *intr_rx;
 	u32 intr_mask_txcmpl;
-	u32 *intr_txcmpl;
+	int *intr_txcmpl;
 	u32 intr_mask_misc;
-	u32 intr_misc;
+	int intr_misc;
 };
 
 /**
@@ -130,6 +130,12 @@ struct edma_intr_info {
  * @has_vports: Virtual ports exist, cached from the PPE variant
  * @vp_inject_port: netdev_arr index that carried the last VP inject
  * @vp_q_mask: CPU-to-Tx-queue mask of the VP inject
+ * @sys_clk: Enabled EDMA system clock
+ * @apb_clk: Enabled EDMA APB clock
+ * @txcmpl_irqs_registered: Number of registered Tx completion IRQs
+ * @rxdesc_irqs_registered: Number of registered Rx descriptor IRQs
+ * @misc_irq_registered: Miscellaneous IRQ is registered
+ * @hw_configured: EDMA rings and hardware are configured
  */
 struct edma_context {
 	struct net_device **netdev_arr;
@@ -150,6 +156,12 @@ struct edma_context {
 	bool has_vports;
 	int vp_inject_port;
 	u32 vp_q_mask;
+	struct clk *sys_clk;
+	struct clk *apb_clk;
+	u32 txcmpl_irqs_registered;
+	u32 rxdesc_irqs_registered;
+	bool misc_irq_registered;
+	bool hw_configured;
 };
 
 struct edma_port_tx_stats;
@@ -159,6 +171,7 @@ extern struct edma_context *edma_ctx;
 
 int edma_err_stats_alloc(void);
 void edma_err_stats_free(void);
+void edma_quiesce(void);
 void edma_destroy(struct ppe_device *ppe_dev);
 int edma_setup(struct ppe_device *ppe_dev);
 void edma_debugfs_teardown(void);
--- a/drivers/net/ethernet/qualcomm/ppe/edma_cfg_rx.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_cfg_rx.c
@@ -408,7 +408,10 @@ static void edma_cfg_rx_fill_ring_cleanu
 	struct regmap *regmap = ppe_dev->regmap;
 	struct device *dev = ppe_dev->dev;
 	u16 cons_idx, curr_idx;
-	u32 data, reg;
+	u32 data = 0, reg;
+
+	if (!rxfill_ring->desc)
+		return;
 
 	/* Get RxFill ring producer index */
 	curr_idx = rxfill_ring->prod_idx & EDMA_RXFILL_PROD_IDX_MASK;
@@ -435,6 +438,8 @@ static void edma_cfg_rx_fill_ring_cleanu
 			continue;
 		}
 
+		edma_rx_dma_unmap(rxfill_ring,
+				  le32_to_cpu((__force __le32)rxfill_desc->word0));
 		dev_kfree_skb_any(skb);
 	}
 
@@ -493,7 +498,10 @@ static void edma_cfg_rx_desc_ring_cleanu
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	struct device *dev = ppe_dev->dev;
-	u32 prod_idx, cons_idx, reg;
+	u32 prod_idx = 0, cons_idx, reg;
+
+	if (!rxdesc_ring->pdesc)
+		return;
 
 	/* Get Rxdesc consumer & producer indices */
 	cons_idx = rxdesc_ring->cons_idx & EDMA_RXDESC_CONS_IDX_MASK;
@@ -519,9 +527,18 @@ static void edma_cfg_rx_desc_ring_cleanu
 			continue;
 		}
 
+		edma_rx_dma_unmap(rxdesc_ring->rxfill,
+				  EDMA_RXDESC_BUFFER_ADDR_GET(rxdesc_pri));
 		dev_kfree_skb_any(skb);
 	}
 
+	/* Reassembled segments have already been unmapped by RX reap. */
+	dev_kfree_skb_any(rxdesc_ring->head);
+	rxdesc_ring->head = NULL;
+	rxdesc_ring->last = NULL;
+	rxdesc_ring->pdesc_head = NULL;
+	rxdesc_ring->vp_punt_src = -1;
+
 	/* Update the consumer index */
 	reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_CONS_IDX(rxdesc_ring->ring_id);
 	regmap_write(regmap, reg, cons_idx);
@@ -546,7 +563,8 @@ static int edma_cfg_rx_rings_setup(void)
 	struct edma_hw_info *hw_info = edma_ctx->hw_info;
 	struct edma_ring_info *rxfill = hw_info->rxfill;
 	struct edma_ring_info *rx = hw_info->rx;
-	u32 ring_idx, alloc_size, buf_len;
+	u32 alloc_size, buf_len;
+	int ring_idx;
 
 	/* Set buffer allocation size */
 	if (edma_ctx->rx_buf_size) {
@@ -597,6 +615,7 @@ static int edma_cfg_rx_rings_setup(void)
 		rxdesc_ring = &edma_ctx->rx_rings[ring_idx];
 		rxdesc_ring->count = EDMA_RX_RING_SIZE;
 		rxdesc_ring->ring_id = rx->ring_start + ring_idx;
+		spin_lock_init(&rxdesc_ring->irq_lock);
 		rxdesc_ring->vp_punt_src = -1;
 
 		if (queue_id > EDMA_CPU_PORT_QUEUE_MAX(EDMA_RX_QUEUE_START)) {
@@ -778,11 +797,11 @@ void edma_cfg_rx_rings_cleanup(void)
 	u32 i;
 
 	/* Free RxFill ring descriptors */
-	for (i = 0; i < rxfill->num_rings; i++)
+	for (i = 0; edma_ctx->rxfill_rings && i < rxfill->num_rings; i++)
 		edma_cfg_rx_fill_ring_cleanup(&edma_ctx->rxfill_rings[i]);
 
 	/* Free Rx completion ring descriptors */
-	for (i = 0; i < rx->num_rings; i++)
+	for (i = 0; edma_ctx->rx_rings && i < rx->num_rings; i++)
 		edma_cfg_rx_desc_ring_cleanup(&edma_ctx->rx_rings[i]);
 
 	kfree(edma_ctx->rxfill_rings);
@@ -827,13 +846,17 @@ void edma_cfg_rx_disable_interrupts(void
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	struct edma_ring_info *rx = hw_info->rx;
+	unsigned long flags;
 	u32 i, reg;
 
 	for (i = 0; i < rx->num_rings; i++) {
 		struct edma_rxdesc_ring *rxdesc_ring =
 				&edma_ctx->rx_rings[i];
+		spin_lock_irqsave(&rxdesc_ring->irq_lock, flags);
+		rxdesc_ring->irq_enabled = false;
 		reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_INT_MASK(rxdesc_ring->ring_id);
 		regmap_write(regmap, reg, EDMA_MASK_INT_CLEAR);
+		spin_unlock_irqrestore(&rxdesc_ring->irq_lock, flags);
 	}
 }
 
@@ -848,13 +871,17 @@ void edma_cfg_rx_enable_interrupts(void)
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	struct edma_ring_info *rx = hw_info->rx;
+	unsigned long flags;
 	u32 i, reg;
 
 	for (i = 0; i < rx->num_rings; i++) {
 		struct edma_rxdesc_ring *rxdesc_ring =
 				&edma_ctx->rx_rings[i];
+		spin_lock_irqsave(&rxdesc_ring->irq_lock, flags);
+		rxdesc_ring->irq_enabled = true;
 		reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_INT_MASK(rxdesc_ring->ring_id);
 		regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_rx);
+		spin_unlock_irqrestore(&rxdesc_ring->irq_lock, flags);
 	}
 }
 
@@ -874,10 +901,11 @@ void edma_cfg_rx_napi_disable(void)
 
 		rxdesc_ring = &edma_ctx->rx_rings[i];
 
-		if (!rxdesc_ring->napi_added)
+		if (!rxdesc_ring->napi_enabled)
 			continue;
 
 		napi_disable(&rxdesc_ring->napi);
+		rxdesc_ring->napi_enabled = false;
 	}
 }
 
@@ -897,10 +925,11 @@ void edma_cfg_rx_napi_enable(void)
 
 		rxdesc_ring = &edma_ctx->rx_rings[i];
 
-		if (!rxdesc_ring->napi_added)
+		if (!rxdesc_ring->napi_added || rxdesc_ring->napi_enabled)
 			continue;
 
 		napi_enable(&rxdesc_ring->napi);
+		rxdesc_ring->napi_enabled = true;
 	}
 }
 
@@ -923,7 +952,10 @@ void edma_cfg_rx_napi_delete(void)
 		if (!rxdesc_ring->napi_added)
 			continue;
 
-		napi_disable(&rxdesc_ring->napi);
+		if (rxdesc_ring->napi_enabled) {
+			napi_disable(&rxdesc_ring->napi);
+			rxdesc_ring->napi_enabled = false;
+		}
 		netif_napi_del(&rxdesc_ring->napi);
 		rxdesc_ring->napi_added = false;
 	}
--- a/drivers/net/ethernet/qualcomm/ppe/edma_cfg_tx.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_cfg_tx.c
@@ -278,7 +278,6 @@ void edma_cfg_tx_rings_disable(u32 port_
 		txdesc_ring = &edma_ctx->tx_rings[ring_idx];
 		u32 data;
 
-		txdesc_ring = &edma_ctx->tx_rings[i];
 		reg = EDMA_BASE_OFFSET + EDMA_REG_TXDESC_CTRL(txdesc_ring->id);
 		regmap_read(regmap, reg, &data);
 		data &= ~EDMA_TXDESC_TX_ENABLE;
@@ -426,6 +425,7 @@ static int edma_cfg_tx_rings_setup(void)
 		txcmpl_ring = &edma_ctx->txcmpl_rings[i];
 		txcmpl_ring->count = EDMA_TX_RING_SIZE;
 		txcmpl_ring->id = txcmpl->ring_start + i;
+		spin_lock_init(&txcmpl_ring->irq_lock);
 		txcmpl_ring->txdesc_ring = &edma_ctx->tx_rings[i];
 
 		ret = edma_cfg_txcmpl_ring_setup(txcmpl_ring);
@@ -555,13 +555,17 @@ void edma_cfg_tx_disable_interrupts(u32
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	struct edma_txcmpl_ring *txcmpl_ring;
+	unsigned long flags;
 	u32 i, ring_idx, reg;
 
 	for_each_possible_cpu(i) {
 		ring_idx = ((port_id - 1) * num_possible_cpus()) + i;
 		txcmpl_ring = &edma_ctx->txcmpl_rings[ring_idx];
+		spin_lock_irqsave(&txcmpl_ring->irq_lock, flags);
+		txcmpl_ring->irq_enabled = false;
 		reg = EDMA_BASE_OFFSET + EDMA_REG_TX_INT_MASK(txcmpl_ring->id);
 		regmap_write(regmap, reg, EDMA_MASK_INT_CLEAR);
+		spin_unlock_irqrestore(&txcmpl_ring->irq_lock, flags);
 	}
 }
 
@@ -575,13 +579,17 @@ void edma_cfg_tx_enable_interrupts(u32 p
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	struct edma_txcmpl_ring *txcmpl_ring;
+	unsigned long flags;
 	u32 i, ring_idx, reg;
 
 	for_each_possible_cpu(i) {
 		ring_idx = ((port_id - 1) * num_possible_cpus()) + i;
 		txcmpl_ring = &edma_ctx->txcmpl_rings[ring_idx];
+		spin_lock_irqsave(&txcmpl_ring->irq_lock, flags);
+		txcmpl_ring->irq_enabled = true;
 		reg = EDMA_BASE_OFFSET + EDMA_REG_TX_INT_MASK(txcmpl_ring->id);
 		regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_txcmpl);
+		spin_unlock_irqrestore(&txcmpl_ring->irq_lock, flags);
 	}
 }
 
@@ -600,10 +608,11 @@ void edma_cfg_tx_napi_enable(u32 port_id
 	for_each_possible_cpu(i) {
 		ring_idx = ((port_id - 1) * num_possible_cpus()) + i;
 		txcmpl_ring = &edma_ctx->txcmpl_rings[ring_idx];
-		if (!txcmpl_ring->napi_added)
+		if (!txcmpl_ring->napi_added || txcmpl_ring->napi_enabled)
 			continue;
 
 		napi_enable(&txcmpl_ring->napi);
+		txcmpl_ring->napi_enabled = true;
 	}
 }
 
@@ -622,10 +631,11 @@ void edma_cfg_tx_napi_disable(u32 port_i
 	for_each_possible_cpu(i) {
 		ring_idx = ((port_id - 1) * num_possible_cpus()) + i;
 		txcmpl_ring = &edma_ctx->txcmpl_rings[ring_idx];
-		if (!txcmpl_ring->napi_added)
+		if (!txcmpl_ring->napi_enabled)
 			continue;
 
 		napi_disable(&txcmpl_ring->napi);
+		txcmpl_ring->napi_enabled = false;
 	}
 }
 
@@ -647,7 +657,10 @@ void edma_cfg_tx_napi_delete(u32 port_id
 		if (!txcmpl_ring->napi_added)
 			continue;
 
-		napi_disable(&txcmpl_ring->napi);
+		if (txcmpl_ring->napi_enabled) {
+			napi_disable(&txcmpl_ring->napi);
+			txcmpl_ring->napi_enabled = false;
+		}
 		netif_napi_del(&txcmpl_ring->napi);
 		txcmpl_ring->napi_added = false;
 	}
--- a/drivers/net/ethernet/qualcomm/ppe/edma_debugfs.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_debugfs.c
@@ -325,13 +325,10 @@ static const struct file_operations edma
 /**
  * edma_debugfs_teardown - EDMA debugfs teardown.
  *
- * EDMA debugfs teardown and free stats memory.
+ * EDMA debugfs teardown.
  */
 void edma_debugfs_teardown(void)
 {
-	/* Free EDMA miscellaneous stats memory */
-	edma_err_stats_free();
-
 	debugfs_remove_recursive(edma_dentry);
 	edma_dentry = NULL;
 	stats_dentry = NULL;
@@ -369,12 +366,6 @@ int edma_debugfs_setup(struct ppe_device
 		goto debugfs_dir_failed;
 	}
 
-	/* Allocate memory for EDMA miscellaneous stats */
-	if (edma_err_stats_alloc() < 0) {
-		pr_err("Unable to allocate miscellaneous percpu stats\n");
-		goto debugfs_dir_failed;
-	}
-
 	if (!debugfs_create_file("err_stats", 0444, stats_dentry,
 				 NULL, &edma_debugfs_misc_file_ops)) {
 		pr_err("Unable to create EDMA miscellaneous statistics file entry in debugfs\n");
--- a/drivers/net/ethernet/qualcomm/ppe/edma_port.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_port.c
@@ -443,9 +443,12 @@ void edma_port_destroy(struct ppe_port *
 	struct net_device *netdev = edma_ctx->netdev_arr[port_id - 1];
 	int ret;
 
+	/* Shared RX has been quiesced by the MAC teardown caller. */
+	unregister_netdev(netdev);
 	edma_port_deconfigure(netdev);
+	edma_ctx->netdev_arr[port_id - 1] = NULL;
+	synchronize_net();
 	edma_port_stats_free(netdev);
-	unregister_netdev(netdev);
 	ret = ppe_port_l3_if_set(port->ppe_dev, port_id, -1);
 	if (ret)
 		netdev_warn(netdev, "failed to unbind L3 interface: %d\n", ret);
@@ -457,7 +460,6 @@ void edma_port_destroy(struct ppe_port *
 		netdev_warn(netdev, "failed to clear L3 interface: %d\n", ret);
 	free_netdev(netdev);
 	ppe_port_phylink_destroy(port);
-	edma_ctx->netdev_arr[port_id - 1] = NULL;
 }
 
 /**
@@ -574,6 +576,7 @@ int edma_port_setup(struct ppe_port *por
 register_netdev_fail:
 	ppe_port_phylink_destroy(port);
 port_phylink_setup_fail:
+	edma_quiesce();
 	edma_port_deconfigure(netdev);
 	edma_ctx->netdev_arr[port_id - 1] = NULL;
 	edma_port_stats_free(netdev);
--- a/drivers/net/ethernet/qualcomm/ppe/edma_rx.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_rx.c
@@ -120,6 +120,18 @@ static int edma_rx_alloc_buffer_list(str
 	return num_alloc;
 }
 
+void edma_rx_dma_unmap(struct edma_rxfill_ring *ring, dma_addr_t dma)
+{
+	struct device *dev = edma_ctx->ppe_dev->dev;
+
+	if (ring->page_mode)
+		dma_unmap_page(dev, dma, PAGE_SIZE, DMA_FROM_DEVICE);
+	else
+		dma_unmap_single(dev, dma,
+				 ring->alloc_size - EDMA_RX_SKB_HEADROOM - NET_IP_ALIGN,
+				 DMA_FROM_DEVICE);
+}
+
 /**
  * edma_rx_alloc_buffer - EDMA Rx alloc buffer.
  * @rxfill_ring: EDMA Rxfill ring
@@ -634,12 +646,10 @@ static struct net_device *edma_rx_get_sr
 static int edma_rx_reap(struct edma_rxdesc_ring *rxdesc_ring, int budget)
 {
 	struct edma_rxdesc_stats *rxdesc_stats = &rxdesc_ring->rxdesc_stats;
-	u32 alloc_size = rxdesc_ring->rxfill->alloc_size;
 	bool page_mode = rxdesc_ring->rxfill->page_mode;
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct edma_rxdesc_pri *next_rxdesc_pri;
 	struct regmap *regmap = ppe_dev->regmap;
-	struct device *dev = ppe_dev->dev;
 	u32 prod_idx, cons_idx, end_idx;
 	u32 work_to_do, work_done = 0;
 	struct sk_buff *next_skb;
@@ -675,17 +685,11 @@ static int edma_rx_reap(struct edma_rxde
 		struct net_device *ndev;
 		struct sk_buff *skb;
 		dma_addr_t dma_addr;
-		u32 dma_map_size;
 
 		skb = next_skb;
 		rxdesc_pri = next_rxdesc_pri;
 		dma_addr = EDMA_RXDESC_BUFFER_ADDR_GET(rxdesc_pri);
-		dma_map_size = alloc_size - EDMA_RX_SKB_HEADROOM - NET_IP_ALIGN;
-
-		if (!page_mode)
-			dma_unmap_single(dev, dma_addr, dma_map_size, DMA_FROM_DEVICE);
-		else
-			dma_unmap_page(dev, dma_addr, PAGE_SIZE, DMA_FROM_DEVICE);
+		edma_rx_dma_unmap(rxdesc_ring->rxfill, dma_addr);
 
 		/* Update consumer index. */
 		cons_idx = (cons_idx + 1) & EDMA_RX_RING_SIZE_MASK;
@@ -811,6 +815,7 @@ int edma_rx_napi_poll(struct napi_struct
 	struct edma_rxdesc_ring *rxdesc_ring = (struct edma_rxdesc_ring *)napi;
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
+	unsigned long flags;
 	int work_done = 0;
 	u32 status, reg;
 
@@ -825,10 +830,13 @@ int edma_rx_napi_poll(struct napi_struct
 		status = status & EDMA_RXDESC_RING_INT_STATUS_MASK;
 	} while (likely(status));
 
-	napi_complete(napi);
-
-	reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_INT_MASK(rxdesc_ring->ring_id);
-	regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_rx);
+	/* Hold through completion: stop cannot race the final IRQ rearm. */
+	spin_lock_irqsave(&rxdesc_ring->irq_lock, flags);
+	if (napi_complete_done(napi, work_done) && rxdesc_ring->irq_enabled) {
+		reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_INT_MASK(rxdesc_ring->ring_id);
+		regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_rx);
+	}
+	spin_unlock_irqrestore(&rxdesc_ring->irq_lock, flags);
 
 	return work_done;
 }
@@ -847,14 +855,18 @@ irqreturn_t edma_rx_handle_irq(int irq,
 	struct edma_rxdesc_ring *rxdesc_ring = (struct edma_rxdesc_ring *)ctx;
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
+	unsigned long flags;
 	u32 reg;
 
-	if (likely(napi_schedule_prep(&rxdesc_ring->napi))) {
+	spin_lock_irqsave(&rxdesc_ring->irq_lock, flags);
+	if (rxdesc_ring->irq_enabled && napi_schedule_prep(&rxdesc_ring->napi)) {
 		/* Disable RxDesc interrupt. */
 		reg = EDMA_BASE_OFFSET + EDMA_REG_RXDESC_INT_MASK(rxdesc_ring->ring_id);
 		regmap_write(regmap, reg, EDMA_MASK_INT_DISABLE);
 		__napi_schedule(&rxdesc_ring->napi);
 	}
 
+	spin_unlock_irqrestore(&rxdesc_ring->irq_lock, flags);
+
 	return IRQ_HANDLED;
 }
--- a/drivers/net/ethernet/qualcomm/ppe/edma_rx.h
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_rx.h
@@ -275,6 +275,10 @@ struct edma_rxdesc_ring {
 	struct edma_rxdesc_stats rxdesc_stats;
 	struct edma_rxfill_ring *rxfill;
 	bool napi_added;
+	bool napi_enabled;
+	/* Serialize poll completion/IRQ scheduling with the final stop mask. */
+	spinlock_t irq_lock;
+	bool irq_enabled;
 	dma_addr_t pdma;
 	dma_addr_t sdma;
 	struct sk_buff *head;
@@ -287,6 +291,7 @@ extern int edma_rx_mitigation_timer;
 extern int edma_rx_mitigation_pkt_cnt;
 
 irqreturn_t edma_rx_handle_irq(int irq, void *ctx);
+void edma_rx_dma_unmap(struct edma_rxfill_ring *ring, dma_addr_t dma);
 int edma_rx_alloc_buffer(struct edma_rxfill_ring *rxfill_ring, int alloc_count);
 int edma_rx_napi_poll(struct napi_struct *napi, int budget);
 #endif
--- a/drivers/net/ethernet/qualcomm/ppe/edma_tx.c
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_tx.c
@@ -331,6 +331,7 @@ int edma_tx_napi_poll(struct napi_struct
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
 	u32 txcmpl_intr_status;
+	unsigned long flags;
 	int work_done = 0;
 	u32 data, reg;
 
@@ -346,11 +347,13 @@ int edma_tx_napi_poll(struct napi_struct
 	} while (txcmpl_intr_status);
 
 	/* No more packets to process. Finish NAPI processing. */
-	napi_complete(napi);
-
-	/* Set Tx cmpl ring interrupt mask. */
-	reg = EDMA_BASE_OFFSET + EDMA_REG_TX_INT_MASK(txcmpl_ring->id);
-	regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_txcmpl);
+	/* Hold through completion: stop cannot race the final IRQ rearm. */
+	spin_lock_irqsave(&txcmpl_ring->irq_lock, flags);
+	if (napi_complete_done(napi, work_done) && txcmpl_ring->irq_enabled) {
+		reg = EDMA_BASE_OFFSET + EDMA_REG_TX_INT_MASK(txcmpl_ring->id);
+		regmap_write(regmap, reg, edma_ctx->intr_info.intr_mask_txcmpl);
+	}
+	spin_unlock_irqrestore(&txcmpl_ring->irq_lock, flags);
 
 	return work_done;
 }
@@ -369,16 +372,20 @@ irqreturn_t edma_tx_handle_irq(int irq,
 	struct edma_txcmpl_ring *txcmpl_ring = (struct edma_txcmpl_ring *)ctx;
 	struct ppe_device *ppe_dev = edma_ctx->ppe_dev;
 	struct regmap *regmap = ppe_dev->regmap;
+	unsigned long flags;
 	u32 reg;
 
 	pr_debug("irq: irq=%d txcmpl_ring_id=%u\n", irq, txcmpl_ring->id);
-	if (likely(napi_schedule_prep(&txcmpl_ring->napi))) {
+	spin_lock_irqsave(&txcmpl_ring->irq_lock, flags);
+	if (txcmpl_ring->irq_enabled && napi_schedule_prep(&txcmpl_ring->napi)) {
 		/* Disable TxCmpl intr. */
 		reg = EDMA_BASE_OFFSET + EDMA_REG_TX_INT_MASK(txcmpl_ring->id);
 		regmap_write(regmap, reg, EDMA_MASK_INT_DISABLE);
 		__napi_schedule(&txcmpl_ring->napi);
 	}
 
+	spin_unlock_irqrestore(&txcmpl_ring->irq_lock, flags);
+
 	return IRQ_HANDLED;
 }
 
--- a/drivers/net/ethernet/qualcomm/ppe/edma_tx.h
+++ b/drivers/net/ethernet/qualcomm/ppe/edma_tx.h
@@ -360,6 +360,10 @@ struct edma_txcmpl_ring {
 	dma_addr_t dma;
 	u32 count;
 	bool napi_added;
+	bool napi_enabled;
+	/* Serialize poll completion/IRQ scheduling with the final stop mask. */
+	spinlock_t irq_lock;
+	bool irq_enabled;
 	struct edma_txdesc_ring *txdesc_ring;
 };
 
--- a/drivers/net/ethernet/qualcomm/ppe/ppe_port.c
+++ b/drivers/net/ethernet/qualcomm/ppe/ppe_port.c
@@ -15,6 +15,7 @@
 #include <linux/reset.h>
 #include <linux/rtnetlink.h>
 
+#include "edma.h"
 #include "edma_port.h"
 #include "ppe.h"
 #include "ppe_flowtable.h"
@@ -1524,6 +1525,7 @@ err_port_clk:
 	ppe_port_clock_deinit(&ppe_ports->port[i]);
 err_port_setup:
 	/* Destroy edma ports created till now */
+	edma_quiesce();
 	while (i) {
 		i--;
 		edma_port_destroy(&ppe_ports->port[i]);
@@ -1548,6 +1550,8 @@ void ppe_port_mac_deinit(struct ppe_devi
 	struct ppe_port *ppe_port;
 	int i;
 
+	edma_quiesce();
+
 	for (i = 0; i < ppe_dev->ports->num; i++) {
 		ppe_port = &ppe_dev->ports->port[i];
 		edma_port_destroy(ppe_port);
