From: ViKing <viking.yfy@qq.com>
Subject: [PATCH] net: ethernet: qualcomm: ppe: manage DSA and PPPoE flow lifecycle

Resolve RTL dual-VLAN, QCA4B VP and physical ingress paths before programming a flow.

Use verified XLT/VSI/L3 transactions, normalized replacement ownership and quarantine on failed teardown. Keep PPPoE activation tied to an eligible flow and restore the fallback path before releasing its session.

Signed-off-by: ViKing <viking.yfy@qq.com>
---
--- a/drivers/net/ethernet/qualcomm/ppe/ppe_flowtable.c
+++ b/drivers/net/ethernet/qualcomm/ppe/ppe_flowtable.c
@@ -24,6 +24,7 @@
 #include <linux/ip.h>
 #include <linux/netdevice.h>
 #include <linux/ppp_channel.h>
+#include <linux/ppp_defs.h>
 #include <linux/rcupdate.h>
 #include <linux/regmap.h>
 #include <linux/rhashtable.h>
@@ -111,7 +112,11 @@
  * @mtu: MTU/MRU owned by the PPPoE L3 interface.
  * @vlan_mtu: MTU of the VLAN/DSA interface below the session.
  * @port: Physical PPE port below the session.
+ * @vp: Retained QCA ingress VP, negative for a physical-port session.
  * @vlan: Active VLAN topology slot, negative until hardware activation.
+ * @l3_if: Independently allocated terminating PPPoE L3 interface.
+ * @my_mac: Router MAC reference retained by the active session.
+ * @vsi_bound: The VLAN VSI and XLT select this session's terminating L3.
  * @svid: Service VLAN below the session, negative when absent.
  * @cvid: Customer VLAN below the session, negative when absent.
  * @keep_svid: Preserve the service tag on packets punted to the CPU.
@@ -134,7 +139,11 @@
 	u16 mtu;
 	u16 vlan_mtu;
 	u8 port;
+	int vp;
 	int vlan;
+	int l3_if;
+	int my_mac;
+	bool vsi_bound;
 	s16 svid;
 	s16 cvid;
 	bool keep_svid;
@@ -158,6 +167,7 @@
  * @keep_svid: Preserve the S-tag on packets punted to the CPU.
  * @keep_cvid: Preserve the C-tag on packets punted to the CPU.
  * @poisoned: Cleanup failed and the slot must not be reused.
+ * @pppoe: Exclusive terminating session, negative for ordinary VLAN routing.
  *
  * A VLAN uplink owns one of the reserved top ingress translation
  * rules, VSIs and an L3 interface, all of its slot index: the rule
@@ -168,6 +178,7 @@
  */
 struct ppe_ft_vlan {
 	struct ppe_ft_ref ref;
+	int pppoe;
 	s16 svid;
 	s16 cvid;
 	u8 port;
@@ -178,6 +189,46 @@
 
 static_assert(offsetof(struct ppe_ft_vlan, ref) == 0);
 
+/* A physical port's L3 binding overrides the L3 interface of a VLAN VSI.
+ * Retain its untagged path while tagged uplinks select L3 through their VSI.
+ * References count VLAN slots, not individual flows.
+ */
+struct ppe_ft_vlan_port {
+	unsigned int refs;
+	int saved_l3;
+	bool poisoned;
+};
+
+struct ppe_ft_data {
+	struct {
+		u8 dst[ETH_ALEN];
+		u8 src[ETH_ALEN];
+		u8 ingress[ETH_ALEN];
+	} eth;
+	__be32 sip;
+	__be32 dip;
+	__be16 sport;
+	__be16 dport;
+	int pppoe_sid;
+	int ingress_pppoe_sid;
+	int ingress_svid;
+	int ingress_cvid;
+	unsigned int ingress_tags;
+	bool ingress_dsa;
+	int ingress_ifindex;
+};
+
+/* Normalized request before hardware indices and references are assigned.
+ * A cookie alone is not proof that a REPLACE still describes the old rule.
+ */
+struct ppe_ft_rule_state {
+	struct ppe_flow_entry entry;
+	struct ppe_flow_nexthop nh;
+	struct ppe_ft_data data;
+	int output_ifindex;
+	int priority;
+};
+
 /**
  * struct ppe_ft_flow - An offloaded flow.
  * @node: Hash table node, keyed by the flow cookie.
@@ -189,6 +240,7 @@
  * @my_mac: Router MAC slot held by the flow, negative when none.
  * @pub_ip: Public IP slot held by the flow, negative when none.
  * @pppoe: PPPoE session slot held by the flow, negative when none.
+ * @ingress_pppoe: Independently held PPPoE ingress session.
  * @vlan: VLAN uplink slot held by the flow, negative when none.
  * @core_egress_port: Core-role egress port held by an RTL DSA flow.
  * @eport: Egress PPE port of the flow.
@@ -202,6 +254,7 @@
 	struct list_head zombie_node;
 	struct rcu_head rcu;
 	unsigned long cookie;
+	struct ppe_ft_rule_state rule;
 	int index;
 	struct ppe_flow_entry entry;
 	u16 nexthop;
@@ -209,12 +262,14 @@
 	int my_mac;
 	int pub_ip;
 	int pppoe;
+	int ingress_pppoe;
 	int vlan;
 	int core_egress_port;
 	int tunnel;
 	int vp;
 	int ivp;
 	struct ppe_vp_flow vp_flow;
+	u8 iport;
 	u8 eport;
 	struct net_device *egress_dev;
 	s16 egress_vid;
@@ -272,6 +327,7 @@
 	PPE_FT_REJECT_EGRESS_VP,
 	PPE_FT_REJECT_NAT,
 	PPE_FT_REJECT_PPPOE,
+	PPE_FT_REJECT_INGRESS_L3,
 	PPE_FT_REJECT_RESOURCE,
 	PPE_FT_REJECT_MAX,
 };
@@ -286,9 +342,37 @@
 	[PPE_FT_REJECT_EGRESS_VP] = "output_vp",
 	[PPE_FT_REJECT_NAT] = "nat",
 	[PPE_FT_REJECT_PPPOE] = "pppoe",
+	[PPE_FT_REJECT_INGRESS_L3] = "input_l3",
 	[PPE_FT_REJECT_RESOURCE] = "resource",
 };
 
+enum ppe_ft_resource_stage {
+	PPE_FT_RES_CORE,
+	PPE_FT_RES_L3_SETUP,
+	PPE_FT_RES_MY_MAC,
+	PPE_FT_RES_NEXTHOP,
+	PPE_FT_RES_HOST,
+	PPE_FT_RES_FLOW_ADD,
+	PPE_FT_RES_FLOW_ENABLE,
+	PPE_FT_RES_FLOW_READBACK,
+	PPE_FT_RES_FLOW_PUBLISH,
+	PPE_FT_RES_VP_PUBLISH,
+	PPE_FT_RES_MAX,
+};
+
+static const char * const ppe_ft_resource_names[PPE_FT_RES_MAX] = {
+	[PPE_FT_RES_CORE] = "core_role",
+	[PPE_FT_RES_L3_SETUP] = "l3_setup",
+	[PPE_FT_RES_MY_MAC] = "my_mac",
+	[PPE_FT_RES_NEXTHOP] = "nexthop",
+	[PPE_FT_RES_HOST] = "host",
+	[PPE_FT_RES_FLOW_ADD] = "flow_add",
+	[PPE_FT_RES_FLOW_ENABLE] = "flow_enable",
+	[PPE_FT_RES_FLOW_READBACK] = "flow_readback",
+	[PPE_FT_RES_FLOW_PUBLISH] = "flow_publish",
+	[PPE_FT_RES_VP_PUBLISH] = "vp_publish",
+};
+
 /* Refcounted hardware host entry. Its hardware-selected index is
  * carried explicitly by every flow which references it.
  */
@@ -303,6 +387,15 @@
 	unsigned int refcnt;
 };
 
+#define PPE_FT_REQUEST_HISTORY 64
+struct ppe_ft_request {
+	struct ppe_flow_entry key;
+	unsigned long cookie;
+	int ingress_ifindex;
+	int result;
+	int index;
+};
+
 /**
  * struct ppe_flowtable - Flowtable offload state of the PPE.
  * @ppe_dev: Back pointer to the PPE device.
@@ -314,6 +407,7 @@
  * @pub_ips: Refcounted public IP slots.
  * @pppoe: Refcounted PPPoE session slots.
  * @vlans: Refcounted VLAN uplink slots.
+ * @vlan_ports: Shared port-L3 selection and untagged fallback ownership.
  * @core_ingress_refs: Users requiring core-role ingress parsing per port.
  * @core_egress_refs: Users requiring core-role egress formatting per port.
  * @ppp_nb: PPP channel lifecycle notifier.
@@ -331,8 +425,12 @@
 	struct dentry *debugfs_active;
 	struct dentry *debugfs_rows;
 	struct dentry *debugfs_pipeline;
+	struct dentry *debugfs_vlan_ingress;
 	struct dentry *debugfs_reasons;
 	struct dentry *debugfs_resources;
+	struct dentry *debugfs_requests;
+	struct ppe_ft_request requests[PPE_FT_REQUEST_HISTORY];
+	u64 request_count;
 	atomic_t ipv4_flows;
 	atomic_t ipv6_flows;
 	atomic_long_t replace_calls;
@@ -342,9 +440,13 @@
 	atomic_long_t flow_index_conflicts;
 	atomic_long_t flow_readback_failures;
 	atomic_long_t flow_stale_replaces;
+	atomic_long_t flow_rule_refreshes;
+	atomic_long_t flow_rule_replaces;
 	atomic_long_t flow_stale_stats;
 	atomic_long_t flow_stale_destroys;
 	atomic_long_t rejects[PPE_FT_REJECT_MAX];
+	unsigned long resource_failures[PPE_FT_RES_MAX][6];
+	int resource_last_errno[PPE_FT_RES_MAX];
 	DECLARE_HASHTABLE(hosts, PPE_FT_HOST_HASH_BITS);
 	struct ppe_ft_nexthop *nexthops;
 	unsigned int nexthop_active;
@@ -355,6 +457,7 @@
 	struct ppe_ft_pub_ip pub_ips[PPE_PUB_IP_TBL_ENTRIES];
 	struct ppe_ft_pppoe pppoe[PPE_FT_PPPOE_SESSIONS];
 	struct ppe_ft_vlan vlans[PPE_L3_VLAN_UPLINKS];
+	struct ppe_ft_vlan_port vlan_ports[PPE_L3_PORT_FALLBACKS];
 	unsigned int core_ingress_refs[PPE_PORT_PARSING_REG_ENTRIES];
 	unsigned int core_egress_refs[PPE_PORT_EG_VLAN_TBL_ENTRIES];
 	struct ppe_ft_tunnel tunnels[PPE_FT_TUNNELS];
@@ -371,6 +474,42 @@
 	.automatic_shrinking = true,
 };
 
+/* Keep allocation, table-command and publication failures distinguishable.
+ * These are control-plane operations under the resource lock, not packets.
+ * No printk: the bounded histogram is available on demand in debugfs.
+ */
+static int ppe_ft_resource_result(struct ppe_flowtable *ft,
+				  enum ppe_ft_resource_stage stage, int ret)
+{
+	int bucket;
+
+	lockdep_assert_held(&ft->lock);
+	if (ret >= 0)
+		return ret;
+	switch (ret) {
+	case -ENOENT:
+		bucket = 0;
+		break;
+	case -ENOSPC:
+		bucket = 1;
+		break;
+	case -ENOMEM:
+		bucket = 2;
+		break;
+	case -EIO:
+		bucket = 3;
+		break;
+	case -ETIMEDOUT:
+		bucket = 4;
+		break;
+	default:
+		bucket = 5;
+	}
+	ft->resource_failures[stage][bucket]++;
+	ft->resource_last_errno[stage] = ret;
+	return ret;
+}
+
 static void ppe_ft_flow_count_inc(struct ppe_flowtable *ft,
 				  const struct ppe_ft_flow *flow)
 {
@@ -393,24 +532,6 @@
  * The Ethernet buffer overlays the destination and source addresses as
  * they appear in the header.
  */
-struct ppe_ft_data {
-	struct {
-		u8 dst[ETH_ALEN];
-		u8 src[ETH_ALEN];
-		u8 ingress[ETH_ALEN];
-	} eth;
-	__be32 sip;
-	__be32 dip;
-	__be16 sport;
-	__be16 dport;
-	int pppoe_sid;
-	int ingress_svid;
-	int ingress_cvid;
-	unsigned int ingress_tags;
-	bool ingress_dsa;
-	int ingress_ifindex;
-};
-
 static int ppe_ft_mangle_eth(const struct flow_action_entry *act, void *eth)
 {
 	const void *src = &act->mangle.val;
@@ -566,11 +687,11 @@
 			   PPE_MY_MAC_TBL_ENTRIES, ppe_ft_my_mac_match,
 			   addr, &is_new);
 	if (i < 0 || !is_new)
-		return i;
+		return ppe_ft_resource_result(ft, PPE_FT_RES_MY_MAC, i);
 
 	ret = ppe_my_mac_set(ft->ppe_dev, i, addr, true);
 	if (ret)
-		return ret;
+		return ppe_ft_resource_result(ft, PPE_FT_RES_MY_MAC, ret);
 
 	ether_addr_copy(ft->my_macs[i].addr, addr);
 	ft->my_macs[i].ref.refcnt = 1;
@@ -711,6 +832,14 @@
 	return ft->ppe_dev->num_ports + index;
 }
 
+static int ppe_ft_pppoe_hw_l3_if(struct ppe_flowtable *ft, int index)
+{
+	struct ppe_ft_pppoe *session = &ft->pppoe[index];
+
+	return session->l3_if >= 0 ? session->l3_if :
+	       ppe_ft_pppoe_l3_if(ft, index);
+}
+
 /* PPP lifecycle topology recovered through the generic forward path. */
 struct ppe_ft_pppoe_topo {
 	u16 sid;
@@ -729,6 +858,9 @@
 			   int cvid, int mtu, bool keep_svid,
 			   bool keep_cvid);
 static void ppe_ft_vlan_put(struct ppe_flowtable *ft, int index);
+static int ppe_ft_vlan_l3_if(struct ppe_flowtable *ft, int index);
+static int ppe_ft_vlan_xlt(int index);
+static int ppe_ft_vlan_vsi(int index);
 
 enum ppe_ft_dsa_mode {
 	PPE_FT_DSA_VLAN,
@@ -886,22 +1018,72 @@
 	       ether_addr_equal(session->peer, want->peer);
 }
 
+/* Withdraw the session and restore VLAN routing before releasing its L3.
+ * A failed write/readback retains every dependency for quarantine.
+ */
+static int ppe_ft_pppoe_deactivate(struct ppe_flowtable *ft, int index)
+{
+	struct ppe_ft_pppoe *session = &ft->pppoe[index];
+	int ret;
+
+	if (!session->hw_active)
+		return 0;
+	ret = ppe_pppoe_session_set(ft->ppe_dev, index, false,
+				    0, NULL, 0, 0);
+	if (ret)
+		return ret;
+	if (session->vsi_bound) {
+		ret = ppe_ing_xlt_l3_if_set(ft->ppe_dev,
+					    ppe_ft_vlan_xlt(session->vlan), -1);
+		if (ret)
+			return ret;
+		ret = ppe_vsi_l3_if_set(ft->ppe_dev,
+					ppe_ft_vlan_vsi(session->vlan),
+					ppe_ft_vlan_l3_if(ft, session->vlan));
+		if (ret)
+			return ret;
+		ft->vlans[session->vlan].pppoe = -1;
+		session->vsi_bound = false;
+	}
+	ret = ppe_l3_if_clear(ft->ppe_dev,
+			      ppe_ft_pppoe_hw_l3_if(ft, index));
+	if (ret)
+		return ret;
+	ppe_ft_my_mac_put(ft, session->my_mac);
+	session->my_mac = -1;
+	session->l3_if = -1;
+	session->hw_active = false;
+	return 0;
+}
+
 /* Activate a connected session when its first eligible IP flow arrives.
  * The PPP notifier runs before LCP/IPCP and must not call this helper.
  * Must be called with the flowtable lock held.
  */
-static int ppe_ft_pppoe_activate(struct ppe_flowtable *ft, int index)
+static int ppe_ft_pppoe_activate(struct ppe_flowtable *ft, int index, int vp)
 {
 	struct ppe_ft_pppoe *session = &ft->pppoe[index];
-	int vlan = -1, ret, cleanup_ret, cleanup_err = 0;
-	bool l3_touched = false, session_touched = false;
+	int vlan = -1, l3_if, mac, ret, cleanup_ret, i;
 
-	if (session->hw_active)
-		return 0;
-	if (!session->connected)
-		return -ENOTCONN;
 	if (session->poisoned)
 		return -EIO;
+	if (!session->connected)
+		return -ENOTCONN;
+	if (session->hw_active)
+		return session->vp == vp ? 0 : -EEXIST;
+
+	/* QCA receive parsing replaces the physical source with a VP. Keep
+	 * that exact mapping alive for as long as the PPPoE row can match it.
+	 * The caller already holds its flow's VP reference.
+	 */
+	if (vp >= 0) {
+		/* VLAN-over-VP needs VP translation rules, not a physical rule. */
+		if (session->svid >= 0 || session->cvid >= 0)
+			return -EOPNOTSUPP;
+		ret = ppe_vp_ref_qca4b(ft->ppe_dev, vp, session->port);
+		if (ret)
+			return ret;
+	}
 
 	if (session->svid >= 0 || session->cvid >= 0) {
 		vlan = ppe_ft_vlan_get(ft, session->port, session->svid,
@@ -915,55 +1097,96 @@
 				session->cvid, vlan);
 			return vlan;
 		}
+		/* A VSI has one L3 selector. Do not redirect another session or
+		 * an already installed plain-IP flow into this session's L3.
+		 */
+		if (ft->vlans[vlan].pppoe >= 0) {
+			ret = -EEXIST;
+			goto err_vlan;
+		}
+		for (i = 0; i < ft->ppe_dev->variant->num_flow_entries; i++) {
+			struct ppe_ft_flow *flow = ft->flow_owners[i];
+
+			if (flow && flow->vlan == vlan && flow->ingress_pppoe < 0) {
+				ret = -EBUSY;
+				goto err_vlan;
+			}
+		}
 	}
 
-	l3_touched = true;
-	ret = ppe_l3_if_set(ft->ppe_dev, ppe_ft_pppoe_l3_if(ft, index),
+	mac = ppe_ft_my_mac_get(ft, session->local);
+	if (mac < 0) {
+		ret = mac;
+		goto err_vlan;
+	}
+	/* Reserve the dependencies before touching hardware, including writes
+	 * that may fail after changing a row. The lock hides partial setup.
+	 */
+	session->vlan = vlan;
+	session->vp = vp;
+	l3_if = ppe_ft_pppoe_l3_if(ft, index);
+	session->l3_if = l3_if;
+	session->my_mac = mac;
+	session->hw_active = true;
+	ret = ppe_l3_if_set(ft->ppe_dev, l3_if,
 			    session->local, session->mtu, session->sid);
 	if (ret)
 		goto err_cleanup;
+	ret = ppe_l3_if_my_mac_set(ft->ppe_dev, l3_if,
+				   mac, true);
+	if (ret)
+		goto err_cleanup;
 
-	session_touched = true;
 	ret = ppe_pppoe_session_set(ft->ppe_dev, index, true,
 				    session->sid, session->peer,
-				    ppe_ft_pppoe_l3_if(ft, index));
+				    l3_if,
+				    vp >= 0 ? vp : session->port);
 	if (ret)
 		goto err_cleanup;
 
-	session->vlan = vlan;
-	session->hw_active = true;
+	if (vlan >= 0) {
+		/* Match the routed PPPoE WAN path: XLT/VSI, session lookup and
+		 * the flow source check must select the same terminating L3.
+		 * CPU tag restoration still belongs to the unchanged VLAN VSI.
+		 */
+		ft->vlans[vlan].pppoe = index;
+		session->vsi_bound = true;
+		ret = ppe_vsi_l3_if_set(ft->ppe_dev, ppe_ft_vlan_vsi(vlan),
+					l3_if);
+		if (ret)
+			goto err_cleanup;
+		/* VSI selection alone did not establish a source L3 on tagged
+		 * PPPoE ingress. Set the explicit XLT source-interface action
+		 * used by QSDK, keeping the S/C key and physical source intact.
+		 */
+		ret = ppe_ing_xlt_l3_if_set(ft->ppe_dev, ppe_ft_vlan_xlt(vlan),
+					    l3_if);
+		if (ret)
+			goto err_cleanup;
+	}
 	dev_info(ft->ppe_dev->dev,
-		 "PPE PPPoE hardware active: slot=%d port=%u svid=%d cvid=%d keep=%u/%u cpu-egress-core-ref=%u\n",
-		 index, session->port, session->svid, session->cvid,
+		 "PPE PPPoE hardware active: slot=%d port=%u vp=%d svid=%d cvid=%d keep=%u/%u cpu-egress-core-ref=%u\n",
+		 index, session->port, vp, session->svid, session->cvid,
 		 session->keep_svid, session->keep_cvid,
 		 ft->core_egress_refs[0]);
 
 	return 0;
 
 err_cleanup:
-	if (session_touched) {
-		cleanup_ret = ppe_pppoe_session_set(ft->ppe_dev, index,
-						    false, 0, NULL, 0);
-		if (cleanup_ret)
-			cleanup_err = cleanup_ret;
-	}
-	if (l3_touched) {
-		cleanup_ret = ppe_l3_if_clear(ft->ppe_dev,
-					      ppe_ft_pppoe_l3_if(ft, index));
-		if (!cleanup_err)
-			cleanup_err = cleanup_ret;
-	}
-	if (cleanup_err) {
-		session->vlan = vlan;
-		session->hw_active = true;
+	cleanup_ret = ppe_ft_pppoe_deactivate(ft, index);
+	if (cleanup_ret) {
 		session->poisoned = true;
 		dev_err(ft->ppe_dev->dev,
 			"quarantining partial PPPoE session %d: setup=%d cleanup=%d\n",
-			index, ret, cleanup_err);
+			index, ret, cleanup_ret);
 		return ret;
 	}
-
+	session->vlan = -1;
+	session->vp = -1;
+	session->l3_if = -1;
+err_vlan:
 	ppe_ft_vlan_put(ft, vlan);
+	ppe_vp_put(ft->ppe_dev, vp);
 	return ret;
 }
 
@@ -971,7 +1194,7 @@
 static int ppe_ft_pppoe_get(struct ppe_flowtable *ft,
 			    struct net_device *ppp_dev, u16 sid,
 			    const u8 *peer, const u8 *local,
-			    int port, int svid, int cvid)
+			    int port, int svid, int cvid, int vp)
 {
 	int i, ret;
 
@@ -991,7 +1214,7 @@
 		    session->svid != svid || session->cvid != cvid ||
 		    (local && !ether_addr_equal(session->local, local)))
 			return -EEXIST;
-		ret = ppe_ft_pppoe_activate(ft, i);
+		ret = ppe_ft_pppoe_activate(ft, i, vp);
 		if (ret)
 			return ret;
 
@@ -1005,21 +1228,14 @@
 static void ppe_ft_pppoe_put(struct ppe_flowtable *ft, int index)
 {
 	struct ppe_ft_pppoe *session;
-	int vlan, ret = 0, cleanup_ret;
+	int vlan, ret;
 
 	if (!ppe_ft_ref_put(ft->pppoe, sizeof(*ft->pppoe), index))
 		return;
 	session = &ft->pppoe[index];
 
 	WARN_ON_ONCE(session->connected);
-	if (session->hw_active) {
-		ret = ppe_pppoe_session_set(ft->ppe_dev, index, false,
-					    0, NULL, 0);
-		cleanup_ret = ppe_l3_if_clear(ft->ppe_dev,
-					      ppe_ft_pppoe_l3_if(ft, index));
-		if (!ret)
-			ret = cleanup_ret;
-	}
+	ret = ppe_ft_pppoe_deactivate(ft, index);
 	if (ret) {
 		session->ref.refcnt = 1;
 		session->connected = false;
@@ -1032,6 +1248,7 @@
 	}
 
 	vlan = session->vlan;
+	ppe_vp_put(ft->ppe_dev, session->vp);
 	memset(session, 0, sizeof(*session));
 	ppe_ft_vlan_put(ft, vlan);
 }
@@ -1054,7 +1271,10 @@
 	want.mtu = topo.mtu;
 	want.vlan_mtu = topo.vlan_mtu;
 	want.port = topo.port;
+	want.vp = -1;
 	want.vlan = -1;
+	want.l3_if = -1;
+	want.my_mac = -1;
 	want.svid = topo.svid;
 	want.cvid = topo.cvid;
 	want.keep_svid = topo.keep_svid;
@@ -1120,7 +1340,7 @@
 static int ppe_ft_pppoe_disconnect(struct ppe_flowtable *ft,
 				   struct net_device *ppp_dev)
 {
-	int i, vlan, ret = -ENOENT, cleanup_ret;
+	int i, vlan, ret = -ENOENT;
 
 	mutex_lock(&ft->lock);
 	for (i = 0; i < PPE_FT_PPPOE_SESSIONS; i++) {
@@ -1131,15 +1351,7 @@
 
 		session->connected = false;
 		session->ppp_dev = NULL;
-		ret = 0;
-		if (session->hw_active) {
-			ret = ppe_pppoe_session_set(ft->ppe_dev, i, false,
-						    0, NULL, 0);
-			cleanup_ret = ppe_l3_if_clear(ft->ppe_dev,
-						      ppe_ft_pppoe_l3_if(ft, i));
-			if (!ret)
-				ret = cleanup_ret;
-		}
+		ret = ppe_ft_pppoe_deactivate(ft, i);
 		if (ret) {
 			/* Retain a phantom lifecycle reference and dependencies so
 			 * an entry whose invalidation failed cannot be reused.
@@ -1288,10 +1500,146 @@
 	return ret;
 }
 
-/* Get a refcounted VLAN uplink slot of (port, SVID, CVID), programming its
- * L3 interface, VSI binding and translation rule on first use. Must
- * be called with the flowtable lock held.
- */
+/* All VLAN and port-fallback transactions hold the flowtable lock. */
+static int ppe_ft_vlan_port_vsi(int port)
+{
+	return PPE_VSI_TBL_ENTRIES - PPE_L3_RESERVED_VSIS + port;
+}
+
+static int ppe_ft_vlan_port_xlt(int port)
+{
+	return PPE_XLT_RULE_TBL_ENTRIES - PPE_L3_RESERVED_VSIS + port;
+}
+
+static int ppe_ft_vlan_port_restore(struct ppe_flowtable *ft, int port)
+{
+	struct ppe_ft_vlan_port *state = &ft->vlan_ports[port];
+	int ret;
+
+	if (state->saved_l3 < 0)
+		return 0;
+
+	/* Restore the port before removing its fallback. A failed restore
+	 * must leave the fallback live, and its reserved resources quarantined.
+	 */
+	ret = ppe_port_l3_if_set(ft->ppe_dev, port, state->saved_l3);
+	if (ret)
+		return ret;
+	ret = ppe_ing_xlt_rule_clear(ft->ppe_dev, ppe_ft_vlan_port_xlt(port));
+	if (ret)
+		return ret;
+	ret = ppe_fdb_vsi_flush(ft->ppe_dev, ppe_ft_vlan_port_vsi(port));
+	if (ret)
+		return ret;
+
+	return ppe_vsi_l3_if_set(ft->ppe_dev, ppe_ft_vlan_port_vsi(port), -1);
+}
+
+static int ppe_ft_vlan_port_get(struct ppe_flowtable *ft, int port)
+{
+	struct ppe_ft_vlan_port *state;
+	u32 reg, val, modes = 0;
+	unsigned int p;
+	int ret, err;
+
+	if (port < 0 || (unsigned int)port >= ARRAY_SIZE(ft->vlan_ports) ||
+	    (unsigned int)port >= ft->ppe_dev->num_ports)
+		return -EINVAL;
+	state = &ft->vlan_ports[port];
+	if (state->poisoned)
+		return -EIO;
+	if (state->refs) {
+		state->refs++;
+		return 0;
+	}
+
+	reg = PPE_L3_VP_PORT_TBL_ADDR + PPE_L3_VP_PORT_TBL_INC * port;
+	ret = regmap_read(ft->ppe_dev->regmap, reg, &val);
+	if (ret)
+		return ret;
+	state->saved_l3 = -1;
+	if (!(val & PPE_L3_VP_PORT_W0_L3_IF_VALID)) {
+		state->refs = 1;
+		return 0;
+	}
+	state->saved_l3 = FIELD_GET(PPE_L3_VP_PORT_W0_L3_IF_INDEX, val);
+
+	/* QSDK detaches the port L3 on the first VLAN and installs an
+	 * untagged translation rule. Otherwise plain routed flows lose their
+	 * source identity. Dedicated slots cannot collide with bridge/VLAN VSIs.
+	 */
+	ret = ppe_vsi_l3_if_set(ft->ppe_dev, ppe_ft_vlan_port_vsi(port),
+				state->saved_l3);
+	if (ret)
+		goto rollback;
+	for (p = 0; p < ft->ppe_dev->num_ports; p++)
+		modes |= PPE_EG_VLAN_MODE_UNMODIFIED << (p * 2);
+	ret = ppe_vsi_eg_mode_set(ft->ppe_dev, ppe_ft_vlan_port_vsi(port), modes);
+	if (ret)
+		goto rollback;
+	ret = ppe_ing_xlt_rule_set(ft->ppe_dev, ppe_ft_vlan_port_xlt(port),
+				   BIT(port), -1, -1, ppe_ft_vlan_port_vsi(port),
+				   false, false);
+	if (ret)
+		goto rollback;
+	ret = ppe_port_l3_if_set(ft->ppe_dev, port, -1);
+	if (ret)
+		goto rollback;
+	state->refs = 1;
+	return 0;
+
+rollback:
+	err = ppe_ft_vlan_port_restore(ft, port);
+	if (err) {
+		state->refs = 1;
+		state->poisoned = true;
+		dev_err(ft->ppe_dev->dev,
+			"quarantining VLAN ingress port %d: %d\n", port, err);
+	} else {
+		memset(state, 0, sizeof(*state));
+	}
+	return ret;
+}
+
+static int ppe_ft_vlan_port_put(struct ppe_flowtable *ft, int port)
+{
+	struct ppe_ft_vlan_port *state = &ft->vlan_ports[port];
+	int ret;
+
+	if (WARN_ON(!state->refs) || state->poisoned)
+		return -EIO;
+	if (state->refs > 1) {
+		state->refs--;
+		return 0;
+	}
+	ret = ppe_ft_vlan_port_restore(ft, port);
+	if (ret) {
+		state->poisoned = true;
+		dev_err(ft->ppe_dev->dev,
+			"quarantining VLAN ingress port %d: %d\n", port, ret);
+		return ret;
+	}
+	memset(state, 0, sizeof(*state));
+	return 0;
+}
+
+static int ppe_ft_port_l3_if_set(struct ppe_flowtable *ft, int port, int l3_if)
+{
+	if ((unsigned int)port < ARRAY_SIZE(ft->vlan_ports)) {
+		struct ppe_ft_vlan_port *state = &ft->vlan_ports[port];
+
+		if (state->poisoned)
+			return -EIO;
+		/* Reverse-direction egress setup must not re-enable the physical
+		 * binding while ingress VLANs still select L3 through their VSI.
+		 */
+		if (state->refs)
+			return 0;
+	}
+
+	return ppe_port_l3_if_set(ft->ppe_dev, port, l3_if);
+}
+
 static bool ppe_ft_vlan_match(const void *entry, const void *key)
 {
 	const struct ppe_ft_vlan *uplink = entry, *want = key;
@@ -1307,6 +1655,7 @@
 			   bool keep_cvid)
 {
 	struct ppe_ft_vlan want = {
+		.pppoe = -1,
 		.svid = svid,
 		.cvid = cvid,
 		.port = port,
@@ -1317,8 +1666,8 @@
 	bool ingress_core = false, cpu_core = false;
 	bool l3_touched = false, vsi_bound = false;
 	bool ingress_touched = false, egress_touched = false;
-	bool strip_svid = svid >= 0 && !keep_svid;
-	bool strip_cvid = cvid >= 0 && !keep_cvid;
+	bool port_owned = false;
+	bool restore_cvid = cvid >= 0 && (svid >= 0 || keep_cvid);
 	u32 eg_modes;
 	bool is_new;
 	int i, p, ret, err = 0, cleanup_ret;
@@ -1369,40 +1718,48 @@
 	if (ret)
 		goto err_config;
 
-	ingress_touched = true;
-	ret = ppe_ing_xlt_rule_set(ft->ppe_dev, ppe_ft_vlan_xlt(i),
-				   BIT(port), svid, cvid,
-				   ppe_ft_vlan_vsi(i),
-				   strip_svid, strip_cvid);
-	if (ret)
-		goto err_config;
-
-	if (strip_svid) {
-		/* Ingress parsing strips the RTL S-tag. Re-add it on the
-		 * PPE CPU port so EDMA and DSA can deliver punted frames.
+	if (svid >= 0 || restore_cvid) {
+		/* QSDK's ordinary routed VLAN path consumes ingress tags.
+		 * DSA still needs its source tag on a CPU punt: prepare
+		 * restoration before publishing the consuming rule. Keep
+		 * the customer tag too when software expects it in-frame.
 		 */
 		egress_touched = true;
 		ret = ppe_eg_xlt_rule_set(ft->ppe_dev, ppe_ft_vlan_xlt(i),
 					  BIT(0), svid, cvid, ppe_ft_vlan_vsi(i),
-					  strip_svid, strip_cvid);
+					  svid >= 0, restore_cvid);
 		if (ret)
 			goto err_config;
 	}
 
+	ret = ppe_ft_vlan_port_get(ft, port);
+	if (ret)
+		goto err_config;
+	port_owned = true;
+
+	/* The flow key uses L3 identity, not the sixth-tuple VLAN mode.
+	 * keep_* describes CPU delivery, not tags in the routed pipeline.
+	 */
+	ingress_touched = true;
+	ret = ppe_ing_xlt_rule_set(ft->ppe_dev, ppe_ft_vlan_xlt(i),
+				   BIT(port), svid, cvid, ppe_ft_vlan_vsi(i),
+				   svid >= 0, cvid >= 0);
+	if (ret)
+		goto err_config;
+
 	want.ref.refcnt = 1;
 	ft->vlans[i] = want;
 
 	return i;
 
 err_config:
-	if (egress_touched)
-		err = ppe_eg_xlt_rule_clear(ft->ppe_dev, ppe_ft_vlan_xlt(i));
 	if (ingress_touched) {
-		cleanup_ret = ppe_ing_xlt_rule_clear(ft->ppe_dev,
-						     ppe_ft_vlan_xlt(i));
-		if (!err)
-			err = cleanup_ret;
+		err = ppe_ing_xlt_rule_clear(ft->ppe_dev, ppe_ft_vlan_xlt(i));
+		if (err)
+			goto quarantine;
 	}
+	if (egress_touched)
+		err = ppe_eg_xlt_rule_clear(ft->ppe_dev, ppe_ft_vlan_xlt(i));
 	if (vsi_bound) {
 		cleanup_ret = ppe_vsi_l3_if_set(ft->ppe_dev,
 						ppe_ft_vlan_vsi(i), -1);
@@ -1415,6 +1772,9 @@
 		if (!err)
 			err = cleanup_ret;
 	}
+	if (!err && port_owned)
+		err = ppe_ft_vlan_port_put(ft, port);
+quarantine:
 	if (err) {
 		want.ref.refcnt = 1;
 		want.poisoned = true;
@@ -1444,10 +1804,15 @@
 
 	slot = &ft->vlans[index];
 	ret = ppe_ing_xlt_rule_clear(ft->ppe_dev, ppe_ft_vlan_xlt(index));
+	/* An active consuming rule must retain CPU restoration and L3.
+	 * Quarantine instead of tearing the punt path out from under it.
+	 */
+	if (ret)
+		goto quarantine;
 	err = ppe_vsi_l3_if_set(ft->ppe_dev, ppe_ft_vlan_vsi(index), -1);
 	if (!ret)
 		ret = err;
-	if (slot->svid >= 0 && !slot->keep_svid) {
+	if (slot->svid >= 0 || (slot->cvid >= 0 && slot->keep_cvid)) {
 		err = ppe_eg_xlt_rule_clear(ft->ppe_dev,
 					    ppe_ft_vlan_xlt(index));
 		if (!ret)
@@ -1464,6 +1829,9 @@
 	err = ppe_l3_if_clear(ft->ppe_dev, ppe_ft_vlan_l3_if(ft, index));
 	if (!ret)
 		ret = err;
+	if (!ret)
+		ret = ppe_ft_vlan_port_put(ft, slot->port);
+quarantine:
 	if (ret) {
 		slot->poisoned = true;
 		slot->ref.refcnt = 1;
@@ -1847,7 +2215,7 @@
 
 	if (have_pppoe) {
 		pppoe = ppe_ft_pppoe_get(ft, ppp_dev, sid, peer, NULL,
-					 tunnel->underlay_port, -1, vid);
+					 tunnel->underlay_port, -1, vid, -1);
 		if (pppoe < 0) {
 			ret = pppoe;
 			goto out_dst;
@@ -2214,6 +2582,7 @@
 	ppe_ft_my_mac_put(ft, flow->my_mac);
 	ppe_ft_my_mac_put(ft, flow->ingress_my_mac);
 	ppe_ft_pppoe_put(ft, flow->pppoe);
+	ppe_ft_pppoe_put(ft, flow->ingress_pppoe);
 	ppe_ft_vlan_put(ft, flow->vlan);
 	ppe_ft_core_port_put(ft, flow->core_egress_port, false);
 	ppe_ft_pub_ip_put(ft, flow->pub_ip);
@@ -2355,6 +2724,41 @@
 	return mode;
 }
 
+/* Return one only when the current request and hardware owner both match. */
+static int ppe_ft_flow_refresh(struct ppe_flowtable *ft,
+			       struct ppe_ft_flow *flow, bool tunnel_flow)
+{
+	struct ppe_ft_flow *old;
+	int ret;
+
+	old = rhashtable_lookup_fast(&ft->flows, &flow->cookie, ppe_ft_params);
+	if (!old)
+		return 0;
+	ret = ppe_flow_entry_matches(ft->ppe_dev, old->index, &old->entry);
+	if (!ret && !tunnel_flow &&
+	    !memcmp(&old->rule, &flow->rule, sizeof(flow->rule))) {
+		atomic_long_inc(&ft->flow_rule_refreshes);
+		return 1;
+	}
+	if (ret && ret != -ENOENT && ret != -ESTALE) {
+		/* Do not report HW_OFFLOAD when readback cannot prove it. */
+		atomic_long_inc(&ft->flow_readback_failures);
+		return ret;
+	}
+	if (!ret) {
+		/* The cookie now describes a different request. Withdraw
+		 * the verified old owner before installing the replacement.
+		 */
+		if (!ppe_ft_flow_hw_del(ft, old))
+			return -EIO;
+		atomic_long_inc(&ft->flow_rule_replaces);
+	} else {
+		atomic_long_inc(&ft->flow_stale_replaces);
+	}
+	ppe_ft_flow_forget(ft, old);
+	return 0;
+}
+
 static int ppe_ft_flow_replace(struct ppe_flowtable *ft,
 			       struct flow_cls_offload *f)
 {
@@ -2377,28 +2781,9 @@
 	int pppoe_pushes = 0, csums = 0;
 	int iport = -1, imtu = 0, egress_core_port = -1;
 
-	/* A refresh of an intact cookie is a no-op. If the hardware has lost
-	 * or recycled its slot, retire the stale object now so the same rule
-	 * can be installed again instead of remaining in software forever.
-	 */
-	mutex_lock(&ft->lock);
-	old = rhashtable_lookup_fast(&ft->flows, &f->cookie, ppe_ft_params);
-	if (old) {
-		ret = ppe_flow_entry_matches(ft->ppe_dev, old->index,
-					     &old->entry);
-		if (!ret) {
-			mutex_unlock(&ft->lock);
-			return 0;
-		}
-		if (ret != -ENOENT && ret != -ESTALE) {
-			atomic_long_inc(&ft->flow_readback_failures);
-			mutex_unlock(&ft->lock);
-			return 0;
-		}
-		atomic_long_inc(&ft->flow_stale_replaces);
-		ppe_ft_flow_forget(ft, old);
-	}
-	mutex_unlock(&ft->lock);
+	/* Padding is included in the normalized request comparison below. */
+	memset(&data, 0, sizeof(data));
+	memset(&nh, 0, sizeof(nh));
 
 	if (!flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_BASIC) ||
 	    !flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_PORTS))
@@ -2428,6 +2813,7 @@
 	      BIT_ULL(FLOW_DISSECTOR_KEY_PORTS) |
 	      BIT_ULL(FLOW_DISSECTOR_KEY_VLAN) |
 	      BIT_ULL(FLOW_DISSECTOR_KEY_CVLAN) |
+	      BIT_ULL(FLOW_DISSECTOR_KEY_PPPOE) |
 	      PPE_FT_TUNNEL_KEYS))
 		return -EOPNOTSUPP;
 
@@ -2442,6 +2828,7 @@
 	flow->my_mac = -1;
 	flow->pub_ip = -1;
 	flow->pppoe = -1;
+	flow->ingress_pppoe = -1;
 	flow->vlan = -1;
 	flow->core_egress_port = -1;
 	flow->tunnel = -1;
@@ -2450,6 +2837,7 @@
 	nh.svid = -1;
 	nh.cvid = -1;
 	data.pppoe_sid = -1;
+	data.ingress_pppoe_sid = -1;
 	data.ingress_svid = -1;
 	data.ingress_cvid = -1;
 	data.ingress_dsa = false;
@@ -2572,6 +2960,22 @@
 		}
 	}
 
+	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_PPPOE)) {
+		struct flow_match_pppoe match;
+
+		flow_rule_match_pppoe(rule, &match);
+		if (match.mask->session_id != htons(0xffff) ||
+		    match.mask->type != htons(0xffff) ||
+		    match.mask->ppp_proto != htons(0xffff) ||
+		    match.key->type != htons(ETH_P_PPP_SES) ||
+		    match.key->ppp_proto != htons(flow->entry.is_ipv6 ?
+						 PPP_IPV6 : PPP_IP)) {
+			ret = -EOPNOTSUPP;
+			goto err_free;
+		}
+		data.ingress_pppoe_sid = ntohs(match.key->session_id);
+	}
+
 	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_META)) {
 		struct flow_match_meta match;
 
@@ -2990,7 +3394,19 @@
 	 * destination of DNAT from the next hop, and the translated L4
 	 * port from the flow entry itself.
 	 */
+	flow->rule.entry = flow->entry;
+	flow->rule.nh = nh;
+	flow->rule.data = data;
+	flow->rule.output_ifindex = odev->ifindex;
+	flow->rule.priority = f->common.prio;
+
 	mutex_lock(&ft->lock);
+	ret = ppe_ft_flow_refresh(ft, flow, tunnel_flow);
+	if (ret) {
+		if (ret > 0)
+			ret = 0;
+		goto err_vp;
+	}
 	if (tunnel_flow) {
 		struct in6_addr local, remote;
 		int link, mtu;
@@ -3071,6 +3487,7 @@
 	reject_reason = PPE_FT_REJECT_RESOURCE;
 	if (egress_core_port >= 0) {
 		ret = ppe_ft_core_port_get(ft, egress_core_port, false);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_CORE, ret);
 		if (ret)
 			goto err_pub_ip;
 		flow->core_egress_port = egress_core_port;
@@ -3094,16 +3511,18 @@
 
 		ret = ppe_l3_if_set(ft->ppe_dev, ingress_l3_if,
 				    data.eth.ingress, imtu, -1);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 		if (ret)
 			goto err_ingress_my_mac;
 
-		ret = ppe_port_l3_if_set(ft->ppe_dev, flow->ivp,
-					 ingress_l3_if);
+		ret = ppe_ft_port_l3_if_set(ft, flow->ivp, ingress_l3_if);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 		if (ret)
 			goto err_ingress_my_mac;
 
 		ret = ppe_l3_if_my_mac_set(ft->ppe_dev, ingress_l3_if,
 					   flow->ingress_my_mac, true);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 		if (ret)
 			goto err_ingress_my_mac;
 	}
@@ -3122,6 +3541,93 @@
 		}
 	}
 
+	/* Pin the input independently of the output rule. An active tagged
+	 * session binds its VLAN VSI to the same L3 used by PPPoE lookup.
+	 */
+	reject_reason = PPE_FT_REJECT_INGRESS_L3;
+	flow->iport = iport;
+	if (!tunnel_flow || tunnel_encap) {
+		int src_l3_if = flow->ivp >= 0 ?
+			ppe_ft_vp_l3_if(ft, flow->ivp) : iport;
+
+		if (flow->vlan >= 0)
+			src_l3_if = ppe_ft_vlan_l3_if(ft, flow->vlan);
+		if (flow->vlan >= 0 && ft->vlans[flow->vlan].pppoe >= 0 &&
+		    data.ingress_pppoe_sid < 0) {
+			ret = -EOPNOTSUPP;
+			goto err_vlan;
+		}
+		if (data.ingress_pppoe_sid >= 0) {
+			int physical = flow->ivp >= 0 ?
+				ppe_vp_physical_port(ft->ppe_dev, flow->ivp) : iport;
+			int session_index = -1;
+
+			for (i = 0; i < PPE_FT_PPPOE_SESSIONS; i++) {
+				struct ppe_ft_pppoe *session = &ft->pppoe[i];
+
+				if (!session->connected || session->poisoned ||
+				    session->sid != data.ingress_pppoe_sid ||
+				    session->port != physical ||
+				    session->svid != data.ingress_svid ||
+				    session->cvid != data.ingress_cvid)
+					continue;
+				if (session_index >= 0) {
+					ret = -EOPNOTSUPP;
+					goto err_vlan;
+				}
+				session_index = i;
+			}
+			if (session_index < 0) {
+				ret = -ENOTCONN;
+				goto err_vlan;
+			}
+			ret = ppe_ft_pppoe_activate(ft, session_index, flow->ivp);
+			if (ret)
+				goto err_vlan;
+			if (flow->vlan != ft->pppoe[session_index].vlan) {
+				ret = -EEXIST;
+				goto err_vlan;
+			}
+			ft->pppoe[session_index].ref.refcnt++;
+			flow->ingress_pppoe = session_index;
+			src_l3_if = ppe_ft_pppoe_hw_l3_if(ft, session_index);
+		}
+		if (src_l3_if < 0 || src_l3_if >= PPE_IN_L3_IF_TBL_ENTRIES) {
+			ret = -EOPNOTSUPP;
+			goto err_vlan;
+		}
+		flow->entry.src_l3_if = src_l3_if;
+		flow->entry.src_l3_if_valid = true;
+
+		/* Bind the MAC to the selected interface even when an ingress VP
+		 * already holds it. PPPoE can override that VP's L3 interface.
+		 */
+		if (is_valid_ether_addr(data.eth.ingress)) {
+			if (flow->ingress_my_mac < 0) {
+				flow->ingress_my_mac =
+					ppe_ft_my_mac_get(ft, data.eth.ingress);
+				if (flow->ingress_my_mac < 0) {
+					ret = flow->ingress_my_mac;
+					goto err_vlan;
+				}
+			}
+			ret = ppe_l3_if_my_mac_set(ft->ppe_dev, src_l3_if,
+						   flow->ingress_my_mac, true);
+			if (ret)
+				goto err_vlan;
+			/* Retain the underlay MAC for ordinary VLAN routing after
+			 * this session withdraws and restores its VSI selector.
+			 */
+			if (flow->vlan >= 0 && flow->ingress_pppoe >= 0) {
+				ret = ppe_l3_if_my_mac_set(ft->ppe_dev,
+							   ppe_ft_vlan_l3_if(ft, flow->vlan),
+							   flow->ingress_my_mac, true);
+				if (ret)
+					goto err_vlan;
+			}
+		}
+	}
+
 	/* A PPPoE push routes the flow through the L3 interface of its
 	 * session, which encapsulates on egress; the session table
 	 * entry of the slot terminates the reverse direction, whose
@@ -3150,12 +3656,13 @@
 
 		flow->pppoe = ppe_ft_pppoe_get(ft, NULL, data.pppoe_sid,
 					       data.eth.dst, data.eth.src,
-					       pppoe_port, nh.svid, nh.cvid);
+					       pppoe_port, nh.svid, nh.cvid,
+					       flow->vp);
 		if (flow->pppoe < 0) {
 			ret = flow->pppoe;
 			goto err_vlan;
 		}
-		nh.l3_if = ppe_ft_pppoe_l3_if(ft, flow->pppoe);
+		nh.l3_if = ppe_ft_pppoe_hw_l3_if(ft, flow->pppoe);
 	}
 	reject_reason = PPE_FT_REJECT_RESOURCE;
 
@@ -3177,6 +3684,7 @@
 		if (data.pppoe_sid < 0) {
 			ret = ppe_l3_if_set(ft->ppe_dev, nh.l3_if,
 					    data.eth.src, odev->mtu, -1);
+			ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 			if (ret)
 				goto err_my_mac;
 		}
@@ -3189,19 +3697,21 @@
 		 * session table instead.
 		 */
 		if (data.pppoe_sid < 0) {
-			ret = ppe_port_l3_if_set(ft->ppe_dev, nh.port,
-						 nh.l3_if);
+			ret = ppe_ft_port_l3_if_set(ft, nh.port, nh.l3_if);
+			ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 			if (ret)
 				goto err_my_mac;
 		}
 
 		ret = ppe_l3_if_my_mac_set(ft->ppe_dev, nh.l3_if,
 					   flow->my_mac, true);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_L3_SETUP, ret);
 		if (ret)
 			goto err_my_mac;
 	}
 
-	nexthop = ppe_ft_nexthop_get(ft, &nh);
+	nexthop = ppe_ft_resource_result(ft, PPE_FT_RES_NEXTHOP,
+					 ppe_ft_nexthop_get(ft, &nh));
 	if (nexthop < 0) {
 		ret = nexthop;
 		goto err_my_mac;
@@ -3210,20 +3720,24 @@
 	flow->nexthop = nexthop;
 	flow->entry.nexthop = nexthop;
 
-	ret = ppe_ft_host_get(ft, &flow->entry);
+	ret = ppe_ft_resource_result(ft, PPE_FT_RES_HOST,
+				     ppe_ft_host_get(ft, &flow->entry));
 	if (ret)
 		goto err_nexthop;
 
-	ret = ppe_flow_entry_add(ft->ppe_dev, &flow->entry);
+	ret = ppe_ft_resource_result(ft, PPE_FT_RES_FLOW_ADD,
+				     ppe_flow_entry_add(ft->ppe_dev, &flow->entry));
 	if (ret < 0)
 		goto err_host;
 	flow->index = ret;
 
 	ret = ppe_flow_entry_enable(ft->ppe_dev, flow->index, true);
+	ret = ppe_ft_resource_result(ft, PPE_FT_RES_FLOW_ENABLE, ret);
 	if (ret)
 		goto err_added_flow;
 
 	ret = ppe_flow_entry_matches(ft->ppe_dev, flow->index, &flow->entry);
+	ret = ppe_ft_resource_result(ft, PPE_FT_RES_FLOW_READBACK, ret);
 	if (ret) {
 		atomic_long_inc(&ft->flow_readback_failures);
 		/* An unknown read failure cannot prove whether the previous or new
@@ -3250,6 +3764,7 @@
 
 	ret = rhashtable_lookup_insert_fast(&ft->flows, &flow->node,
 					    ppe_ft_params);
+	ret = ppe_ft_resource_result(ft, PPE_FT_RES_FLOW_PUBLISH, ret);
 	if (ret)
 		goto err_hw_flow;
 	ppe_ft_flow_count_inc(ft, flow);
@@ -3257,6 +3772,7 @@
 	if (flow->ivp >= 0) {
 		ret = ppe_vp_flow_add(ft->ppe_dev, flow->ivp,
 				      &flow->vp_flow);
+		ret = ppe_ft_resource_result(ft, PPE_FT_RES_VP_PUBLISH, ret);
 		if (ret) {
 			rhashtable_remove_fast(&ft->flows, &flow->node,
 					       ppe_ft_params);
@@ -3305,6 +3821,7 @@
 err_pppoe:
 	ppe_ft_pppoe_put(ft, flow->pppoe);
 err_vlan:
+	ppe_ft_pppoe_put(ft, flow->ingress_pppoe);
 	ppe_ft_vlan_put(ft, flow->vlan);
 err_ingress_my_mac:
 	ppe_ft_my_mac_put(ft, flow->ingress_my_mac);
@@ -3583,6 +4100,8 @@
 {
 	if (cpu) {
 		switch (code) {
+		case 89:
+			return "L3 flow source interface check failed";
 		case 148:
 			return "L3 route host mismatch action";
 		case 162:
@@ -3732,6 +4251,27 @@
 			   !!(port_row[1] & PPE_L3_VP_PORT_W1_MAC_VALID), mac);
 		ppe_ft_debug_words(seq, "l3_vp_port_raw", port_row,
 				   ARRAY_SIZE(port_row));
+		if (port < ARRAY_SIZE(ft->vlan_ports)) {
+			struct ppe_ft_vlan_port *state = &ft->vlan_ports[port];
+			u32 vsi_l3 = 0, xlt[4] = {};
+			int vsi_err, xlt_err;
+
+			reg = PPE_L3_VSI_ADDR + PPE_L3_VSI_INC *
+			      ppe_ft_vlan_port_vsi(port);
+			vsi_err = regmap_read(ft->ppe_dev->regmap, reg, &vsi_l3);
+			reg = PPE_XLT_RULE_TBL_ADDR + PPE_XLT_RULE_TBL_INC *
+			      ppe_ft_vlan_port_xlt(port);
+			xlt_err = regmap_bulk_read(ft->ppe_dev->regmap, reg,
+						   xlt, ARRAY_SIZE(xlt));
+			seq_printf(seq,
+				   "vlan_port index:%d refs:%u saved_l3:%d poisoned:%u fallback_vsi:%d l3_raw:%08x xlt:%d read:%d/%d\n",
+				   port, state->refs, state->saved_l3,
+				   state->poisoned, ppe_ft_vlan_port_vsi(port),
+				   vsi_l3, ppe_ft_vlan_port_xlt(port),
+				   vsi_err, xlt_err);
+			ppe_ft_debug_words(seq, "untagged_xlt_raw", xlt,
+					   ARRAY_SIZE(xlt));
+		}
 
 		if (port_err ||
 		    !(port_row[0] & PPE_L3_VP_PORT_W0_L3_IF_VALID))
@@ -3775,6 +4315,172 @@
 }
 DEFINE_SHOW_ATTRIBUTE(ppe_ft_pipeline_state);
 
+/* Bounded, on-demand XLT hit evidence; no per-packet logging or writes. */
+static void ppe_ft_xlt_count_show(struct seq_file *seq,
+				  struct ppe_flowtable *ft, int index)
+{
+	u32 rule[4] = {}, action[3] = {}, count[3] = {};
+	u32 eg_rule[2] = {}, eg_action[3] = {};
+	int rule_err, action_err, count_err;
+	int eg_rule_err, eg_action_err;
+	u32 reg, counter_id;
+	u64 bytes;
+
+	reg = PPE_XLT_RULE_TBL_ADDR + PPE_XLT_RULE_TBL_INC * index;
+	rule_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, rule,
+				    ARRAY_SIZE(rule));
+	reg = PPE_XLT_ACTION_TBL_ADDR + PPE_XLT_ACTION_TBL_INC * index;
+	action_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, action,
+				      ARRAY_SIZE(action));
+	reg = PPE_XLT_CNT_TBL_ADDR + PPE_XLT_CNT_TBL_INC * index;
+	count_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, count,
+				     ARRAY_SIZE(count));
+	counter_id = FIELD_GET(PPE_XLT_ACTION_W1_CNT_ID_LO, action[1]) |
+		     FIELD_GET(PPE_XLT_ACTION_W2_CNT_ID_HI, action[2]) << 3;
+	bytes = count[1] | (u64)FIELD_GET(PPE_XLT_CNT_W2_BYTES_HI, count[2]) << 32;
+	seq_printf(seq,
+		   " xlt:%d read:%d/%d/%d valid:%u counter_en:%u counter_id:%u vsi:%lu strip:%lu/%lu packets:%u bytes:%llu\n",
+		   index, rule_err, action_err, count_err,
+		   !!(rule[0] & PPE_XLT_RULE_W0_VALID),
+		   !!(action[1] & PPE_XLT_ACTION_W1_CNT_EN), counter_id,
+		   FIELD_GET(PPE_XLT_ACTION_W1_VSI, action[1]),
+		   FIELD_GET(PPE_XLT_ACTION_W0_XLT_SVID_CMD, action[0]),
+		   FIELD_GET(PPE_XLT_ACTION_W0_XLT_CVID_CMD, action[0]),
+		   count[0], (unsigned long long)bytes);
+	ppe_ft_debug_words(seq, "  ingress_rule", rule, ARRAY_SIZE(rule));
+	ppe_ft_debug_words(seq, "  ingress_action", action, ARRAY_SIZE(action));
+	seq_printf(seq, "  source_override valid:%u l3_type:%u index:%lu\n",
+		   !!(action[1] & PPE_XLT_ACTION_W1_SRC_INFO_VALID),
+		   !!(action[1] & PPE_XLT_ACTION_W1_SRC_INFO_L3),
+		   FIELD_GET(PPE_XLT_ACTION_W1_SRC_INFO, action[1]));
+	reg = PPE_EG_XLT_RULE_TBL_ADDR + PPE_EG_XLT_RULE_TBL_INC * index;
+	eg_rule_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, eg_rule,
+				       ARRAY_SIZE(eg_rule));
+	reg = PPE_EG_XLT_ACTION_TBL_ADDR + PPE_EG_XLT_ACTION_TBL_INC * index;
+	eg_action_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, eg_action,
+					 ARRAY_SIZE(eg_action));
+	seq_printf(seq, "  egress xlt:%d read:%d/%d valid:%u ports:%#lx vsi:%lu\n",
+		   index, eg_rule_err, eg_action_err,
+		   !!(eg_rule[0] & PPE_EG_XLT_RULE_W0_VALID),
+		   FIELD_GET(PPE_EG_XLT_RULE_W0_PORT_BITMAP, eg_rule[0]),
+		   FIELD_GET(PPE_EG_XLT_RULE_W0_VSI, eg_rule[0]));
+	ppe_ft_debug_words(seq, "  egress_rule", eg_rule, ARRAY_SIZE(eg_rule));
+	ppe_ft_debug_words(seq, "  egress_action", eg_action, ARRAY_SIZE(eg_action));
+}
+
+static void ppe_ft_l3_counter_show(struct seq_file *seq,
+				   struct ppe_flowtable *ft, int l3_if)
+{
+	u32 row[5] = {}, reg, drop_packets;
+	u64 bytes, drop_bytes;
+	int ret;
+
+	reg = PPE_RT_IF_CNT_TBL_ADDR + PPE_RT_IF_CNT_TBL_INC * l3_if;
+	ret = regmap_bulk_read(ft->ppe_dev->regmap, reg, row, ARRAY_SIZE(row));
+	if (ret) {
+		seq_printf(seq, "l3_counter index:%d read:%d\n", l3_if, ret);
+		return;
+	}
+	bytes = row[1] |
+		(u64)FIELD_GET(PPE_RT_IF_CNT_W2_BYTES_HI, row[2]) << 32;
+	drop_packets = FIELD_GET(PPE_RT_IF_CNT_W2_DROP_PACKETS_LO, row[2]) |
+		FIELD_GET(PPE_RT_IF_CNT_W3_DROP_PACKETS_HI, row[3]) << 24;
+	drop_bytes = FIELD_GET(PPE_RT_IF_CNT_W3_DROP_BYTES_LO, row[3]) |
+		(u64)FIELD_GET(PPE_RT_IF_CNT_W4_DROP_BYTES_HI, row[4]) << 24;
+	seq_printf(seq,
+		   "l3_counter index:%d read:0 packets:%u bytes:%llu drop_packets:%u drop_bytes:%llu\n",
+		   l3_if, row[0], bytes, drop_packets, drop_bytes);
+}
+
+static int ppe_ft_vlan_ingress_show(struct seq_file *seq, void *data)
+{
+	struct ppe_flowtable *ft = seq->private;
+	int i;
+
+	mutex_lock(&ft->lock);
+	for (i = 0; i < PPE_L3_VLAN_UPLINKS; i++) {
+		const struct ppe_ft_vlan *vlan = &ft->vlans[i];
+
+		if (!vlan->ref.refcnt)
+			continue;
+		seq_printf(seq,
+			   "vlan index:%d port:%u svid:%d cvid:%d ref:%u poisoned:%u l3:%d",
+			   i, vlan->port, vlan->svid, vlan->cvid,
+			   vlan->ref.refcnt, vlan->poisoned,
+			   ppe_ft_vlan_l3_if(ft, i));
+		ppe_ft_xlt_count_show(seq, ft, ppe_ft_vlan_xlt(i));
+		ppe_ft_l3_counter_show(seq, ft, ppe_ft_vlan_l3_if(ft, i));
+	}
+	for (i = 1; i < ft->ppe_dev->num_ports; i++) {
+		const struct ppe_ft_vlan_port *port = &ft->vlan_ports[i];
+
+		if (!port->refs || port->saved_l3 < 0)
+			continue;
+		seq_printf(seq, "fallback port:%d ref:%u poisoned:%u l3:%d",
+			   i, port->refs, port->poisoned, port->saved_l3);
+		ppe_ft_xlt_count_show(seq, ft, ppe_ft_vlan_port_xlt(i));
+		ppe_ft_l3_counter_show(seq, ft, port->saved_l3);
+	}
+	for (i = 0; i < PPE_FT_PPPOE_SESSIONS; i++)
+		if (ft->pppoe[i].hw_active)
+			ppe_ft_l3_counter_show(seq, ft, ppe_ft_pppoe_hw_l3_if(ft, i));
+	mutex_unlock(&ft->lock);
+
+	return 0;
+}
+DEFINE_SHOW_ATTRIBUTE(ppe_ft_vlan_ingress);
+
+static void ppe_ft_input_l3_show(struct seq_file *seq, struct ppe_flowtable *ft,
+				 const struct ppe_ft_flow *flow)
+{
+	u32 l3[3] = {}, port_row[4] = {}, vsi_row = 0;
+	int port = flow->ivp >= 0 ? flow->ivp : flow->iport;
+	int l3_err, port_err, vsi_err;
+	u32 reg;
+
+	reg = PPE_IN_L3_IF_TBL_ADDR +
+	      PPE_IN_L3_IF_TBL_INC * flow->entry.src_l3_if;
+	l3_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, l3, ARRAY_SIZE(l3));
+	reg = PPE_L3_VP_PORT_TBL_ADDR + PPE_L3_VP_PORT_TBL_INC * port;
+	port_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, port_row,
+				    ARRAY_SIZE(port_row));
+	seq_printf(seq,
+		   "  input_l3 expected:%u valid:%u pppoe_slot:%d mac_slot:%d read:%d bitmap:%02lx raw:%08x/%08x/%08x\n",
+		   flow->entry.src_l3_if, flow->entry.src_l3_if_valid,
+		   flow->ingress_pppoe, flow->ingress_my_mac, l3_err,
+		   FIELD_GET(PPE_IN_L3_IF_W1_MAC_BITMAP, l3[1]),
+		   l3[0], l3[1], l3[2]);
+	seq_printf(seq,
+		   "  input_port port:%d read:%d l3_valid:%u l3:%lu vsi_valid:%u vsi:%lu mac_valid:%u\n",
+		   port, port_err, !!(port_row[0] & PPE_L3_VP_PORT_W0_L3_IF_VALID),
+		   FIELD_GET(PPE_L3_VP_PORT_W0_L3_IF_INDEX, port_row[0]),
+		   !!(port_row[1] & PPE_L3_VP_PORT_W1_VSI_VALID),
+		   FIELD_GET(PPE_L3_VP_PORT_W1_VSI, port_row[1]),
+		   !!(port_row[1] & PPE_L3_VP_PORT_W1_MAC_VALID));
+	if (flow->vlan >= 0) {
+		int vsi = ppe_ft_vlan_vsi(flow->vlan);
+		int vlan_l3 = ppe_ft_vlan_l3_if(ft, flow->vlan);
+
+		reg = PPE_L3_VSI_ADDR + PPE_L3_VSI_INC * vsi;
+		vsi_err = regmap_read(ft->ppe_dev->regmap, reg, &vsi_row);
+		seq_printf(seq,
+			   "  input_vsi vsi:%d read:%d valid:%u l3:%lu raw:%08x\n",
+			   vsi, vsi_err, !!(vsi_row & PPE_L3_VSI_L3_IF_VALID),
+			   FIELD_GET(PPE_L3_VSI_L3_IF_INDEX, vsi_row), vsi_row);
+		reg = PPE_IN_L3_IF_TBL_ADDR + PPE_IN_L3_IF_TBL_INC * vlan_l3;
+		l3_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, l3,
+					  ARRAY_SIZE(l3));
+		seq_printf(seq,
+			   "  input_vlan_l3 index:%d read:%d bitmap:%02lx v4_route:%u v6_route:%u pppoe:%u raw:%08x/%08x/%08x\n",
+			   vlan_l3, l3_err,
+			   FIELD_GET(PPE_IN_L3_IF_W1_MAC_BITMAP, l3[1]),
+			   !!(l3[0] & PPE_IN_L3_IF_W0_IPV4_UC_ROUTE_EN),
+			   !!(l3[0] & PPE_IN_L3_IF_W0_IPV6_UC_ROUTE_EN),
+			   !!(l3[1] & PPE_IN_L3_IF_W1_PPPOE_EN),
+			   l3[0], l3[1], l3[2]);
+	}
+}
+
 static int ppe_ft_flow_rows_show(struct seq_file *seq, void *data)
 {
 	struct ppe_flowtable *ft = seq->private;
@@ -3812,7 +4518,7 @@
 
 			if (!session->ref.refcnt)
 				continue;
-			l3_if = ppe_ft_pppoe_l3_if(ft, i);
+			l3_if = ppe_ft_pppoe_hw_l3_if(ft, i);
 			reg = PPE_PPPOE_SESSION_ADDR + PPE_PPPOE_SESSION_INC * i;
 			base_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, base,
 						    ARRAY_SIZE(base));
@@ -3832,22 +4538,38 @@
 			eg_err = regmap_bulk_read(ft->ppe_dev->regmap, reg, eg_l3,
 						  ARRAY_SIZE(eg_l3));
 			seq_printf(seq,
-				   "pppoe index:%d sid:%u port:%u connected:%u hw_active:%u poisoned:%u ref:%u svid:%d cvid:%d keep:%u/%u peer:%pM local:%pM l3_if:%d\n",
-				   i, session->sid, session->port,
+				   "pppoe index:%d sid:%u port:%u vp:%d connected:%u hw_active:%u poisoned:%u ref:%u svid:%d cvid:%d keep:%u/%u peer:%pM local:%pM l3_if:%d\n",
+				   i, session->sid, session->port, session->vp,
 				   session->connected, session->hw_active,
 				   session->poisoned, session->ref.refcnt,
 				   session->svid, session->cvid,
 				   session->keep_svid, session->keep_cvid,
 				   session->peer,
 				   session->local, l3_if);
+			if (session->vlan >= 0) {
+				u32 vsi = 0;
+				int vsi_err;
+
+				reg = PPE_L3_VSI_ADDR + PPE_L3_VSI_INC *
+				      ppe_ft_vlan_vsi(session->vlan);
+				vsi_err = regmap_read(ft->ppe_dev->regmap, reg, &vsi);
+				seq_printf(seq,
+					   "  pppoe_vlan slot:%d owner:%d bound:%u mac_slot:%d vsi:%d read:%d valid:%u l3:%lu expected:%d\n",
+					   session->vlan, ft->vlans[session->vlan].pppoe,
+					   session->vsi_bound, session->my_mac,
+					   ppe_ft_vlan_vsi(session->vlan), vsi_err,
+					   !!(vsi & PPE_L3_VSI_L3_IF_VALID),
+					   FIELD_GET(PPE_L3_VSI_L3_IF_INDEX, vsi), l3_if);
+			}
 			ppe_ft_debug_words(seq, "pppoe_base_raw", base,
 					   ARRAY_SIZE(base));
 			seq_printf(seq,
-				   "  pppoe_ext_raw:%08x %08x %08x read:%d/%d/%d/%d sid:%lu port_bitmap:%02lx uc:%u smac:%u l3_valid:%u l3:%lu\n",
+				   "  pppoe_ext_raw:%08x %08x %08x read:%d/%d/%d/%d sid:%lu port_type:%lu port_value:%02lx uc:%u smac:%u l3_valid:%u l3:%lu\n",
 				   ext, ext1, ext2, base_err, ext_err,
 				   ext1_err, ext2_err,
 				   FIELD_GET(PPE_PPPOE_SESSION_SESSION_ID,
 					     base[0]),
+				   FIELD_GET(PPE_PPPOE_SESSION_PORT_TYPE, base[0]),
 				   FIELD_GET(PPE_PPPOE_SESSION_PORT_BITMAP,
 					     base[0]),
 				   !!(ext & PPE_PPPOE_SESSION_EXT_UC_VALID),
@@ -3937,7 +4659,7 @@
 		} host_ip = {}, inline_ip = {};
 		__be32 dnat_ip, pub_ip_addr;
 		u8 dmac[ETH_ALEN];
-		int flow_err, host_err, nh_err, in_err, eg_err, qos_err;
+		int flow_err, host_err, nh_err, in_err, eg_err, qos_err, stats_err;
 		int my_mac_err = 0, pub_ip_err = 0;
 		int flow_words, host_words;
 		bool key_match, source_match;
@@ -3989,7 +4711,8 @@
 			pub_ip_err = regmap_read(ft->ppe_dev->regmap, reg,
 						 &pub_ip);
 		}
-		ppe_flow_stats_get(ft->ppe_dev, flow->index, &packets, &bytes);
+		stats_err = ppe_flow_stats_get(ft->ppe_dev, flow->index,
+					       &packets, &bytes);
 
 		if (flow->entry.is_ipv6) {
 			ppe_ft_host_ip6_decode(&host_ip.ip6, h);
@@ -4041,7 +4764,7 @@
 				   &flow->entry.dip.ip4,
 				   be16_to_cpu(flow->entry.dport));
 		seq_printf(seq,
-			   " proto:%u fwd:%u priority:%u priority_valid:%u hw_priority:%u hw_priority_valid:%u hw_profile:%u index:%d host:%u nexthop:%u my_mac:%d pub_ip:%d packets:%u bytes:%llu\n",
+			   " proto:%u fwd:%u priority:%u priority_valid:%u hw_priority:%u hw_priority_valid:%u hw_profile:%u index:%d host:%u nexthop:%u my_mac:%d pub_ip:%d stats_read:%d packets:%u bytes:%llu\n",
 			   flow->entry.proto, flow->entry.fwd_type,
 			   flow->entry.priority, flow->entry.priority_valid,
 			   hw_priority,
@@ -4049,8 +4772,9 @@
 			   hw_profile, flow->index,
 			   flow->entry.host_index,
 			   flow->nexthop,
-			   flow->my_mac, flow->pub_ip, packets,
+			   flow->my_mac, flow->pub_ip, stats_err, packets,
 			   (unsigned long long)bytes);
+		ppe_ft_input_l3_show(seq, ft, flow);
 		ppe_ft_debug_words(seq, "flow_raw", f, flow_words);
 		seq_printf(seq,
 			   "  flow_dec read:%d qos_read:%d valid:%u type:%u host_dst:%u host:%u proto:%lu age:%lu src_l3_valid:%u src_l3:%lu fwd:%lu nexthop:%u nat_l4:%lu profile:%u service:%lu",
@@ -4164,6 +4888,9 @@
 		seq_printf(seq, " %s:%ld", ppe_ft_reject_names[i],
 			   atomic_long_read(&ft->rejects[i]));
 	seq_putc(seq, '\n');
+	seq_printf(seq, "rule refresh:%ld replace:%ld\n",
+		   atomic_long_read(&ft->flow_rule_refreshes),
+		   atomic_long_read(&ft->flow_rule_replaces));
 
 	rhashtable_walk_enter(&ft->flows, &iter);
 	rhashtable_walk_start(&iter);
@@ -4171,6 +4898,7 @@
 		u32 f[3] = {}, packets = 0, qos[2] = {};
 		u64 bytes = 0;
 		u8 hw_priority, hw_profile;
+		int flow_err, qos_err, stats_err;
 
 		if (IS_ERR(flow)) {
 			if (PTR_ERR(flow) == -EAGAIN)
@@ -4178,18 +4906,19 @@
 			break;
 		}
 
-		regmap_bulk_read(ft->ppe_dev->regmap,
-				 PPE_FLOW_TBL_ADDR +
-				 PPE_FLOW_TBL_INC * flow->index,
-				 f, ARRAY_SIZE(f));
+		flow_err = regmap_bulk_read(ft->ppe_dev->regmap,
+					    PPE_FLOW_TBL_ADDR +
+					    PPE_FLOW_TBL_INC * flow->index,
+					    f, ARRAY_SIZE(f));
 		hw_profile = FIELD_GET(PPE_FLOW_W1_PRI_PROFILE_LO, f[1]) |
 			     FIELD_GET(PPE_FLOW_W2_PRI_PROFILE_HI, f[2]) << 1;
-		regmap_bulk_read(ft->ppe_dev->regmap,
-				 PPE_QOS_MAPPING_TBL_ADDR +
-				 PPE_QOS_MAPPING_TBL_INC * flow->index,
-				 qos, ARRAY_SIZE(qos));
+		qos_err = regmap_bulk_read(ft->ppe_dev->regmap,
+					   PPE_QOS_MAPPING_TBL_ADDR +
+					   PPE_QOS_MAPPING_TBL_INC * flow->index,
+					   qos, ARRAY_SIZE(qos));
 		hw_priority = FIELD_GET(PPE_QOS_MAPPING_W0_INT_PRI, qos[0]);
-		ppe_flow_stats_get(ft->ppe_dev, flow->index, &packets, &bytes);
+		stats_err = ppe_flow_stats_get(ft->ppe_dev, flow->index,
+					       &packets, &bytes);
 
 		if (flow->entry.is_ipv6)
 			seq_printf(seq, "[%pI6c]:%u -> [%pI6c]:%u",
@@ -4204,14 +4933,14 @@
 				   &flow->entry.dip.ip4,
 				   be16_to_cpu(flow->entry.dport));
 		seq_printf(seq,
-			   " proto:%u priority:%u priority_valid:%u hw_priority:%u hw_priority_valid:%u hw_profile:%u index:%d valid:%u host:%u host_dst:%u nexthop:%u ivp:%d vp:%d packets:%u bytes:%llu\n",
+			   " proto:%u priority:%u priority_valid:%u hw_priority:%u hw_priority_valid:%u hw_profile:%u index:%d valid:%u host:%u host_dst:%u nexthop:%u ivp:%d vp:%d flow_read:%d qos_read:%d stats_read:%d packets:%u bytes:%llu\n",
 			   flow->entry.proto, flow->entry.priority,
 			   flow->entry.priority_valid, hw_priority,
 			   !!(qos[0] & PPE_QOS_MAPPING_W0_INT_PRI_EN), hw_profile,
 			   flow->index, !!(f[0] & PPE_FLOW_W0_VALID),
 			   flow->entry.host_index,
 			   !!(f[0] & PPE_FLOW_W0_HOST_INDEX_TYPE), flow->nexthop,
-			   flow->ivp, flow->vp,
+			   flow->ivp, flow->vp, flow_err, qos_err, stats_err,
 			   packets, (unsigned long long)bytes);
 	}
 	rhashtable_walk_stop(&iter);
@@ -4241,9 +4970,26 @@
 static int ppe_ft_resource_stats_show(struct seq_file *seq, void *data)
 {
 	struct ppe_flowtable *ft = seq->private;
-	int port;
+	struct ppe_ft_host *host;
+	unsigned int hosts = 0;
+	int port, bucket;
 
 	mutex_lock(&ft->lock);
+	hash_for_each(ft->hosts, bucket, host, node)
+		hosts++;
+	seq_printf(seq, "host active:%u capacity:%u flow active:%d/%d capacity:%u\n",
+		   hosts, ft->ppe_dev->variant->num_host_entries,
+		   atomic_read(&ft->ipv4_flows), atomic_read(&ft->ipv6_flows),
+		   ft->ppe_dev->variant->num_flow_entries);
+	for (bucket = 0; bucket < PPE_FT_RES_MAX; bucket++) {
+		unsigned long *counts = ft->resource_failures[bucket];
+
+		seq_printf(seq,
+			   "failure stage:%s enoent:%lu enospc:%lu enomem:%lu eio:%lu timeout:%lu other:%lu last_errno:%d\n",
+			   ppe_ft_resource_names[bucket], counts[0], counts[1],
+			   counts[2], counts[3], counts[4], counts[5],
+			   ft->resource_last_errno[bucket]);
+	}
 	seq_printf(seq,
 		   "nexthop active:%u capacity:%u alloc:%lu reuse:%lu free:%lu\n",
 		   ft->nexthop_active,
@@ -4274,6 +5020,35 @@
 }
 DEFINE_SHOW_ATTRIBUTE(ppe_ft_resource_stats);
 
+static int ppe_ft_requests_show(struct seq_file *seq, void *data)
+{
+	struct ppe_flowtable *ft = seq->private;
+	u64 first, id;
+
+	mutex_lock(&ft->lock);
+	first = ft->request_count - min_t(u64, ft->request_count,
+					PPE_FT_REQUEST_HISTORY);
+	for (id = first; id < ft->request_count; id++) {
+		const struct ppe_ft_request *r =
+			&ft->requests[id % PPE_FT_REQUEST_HISTORY];
+
+		seq_printf(seq, "request:%llu cookie:%lx iif:%d result:%d index:%d ",
+			   id, r->cookie, r->ingress_ifindex, r->result, r->index);
+		if (r->key.is_ipv6)
+			seq_printf(seq, "[%pI6c]:%u -> [%pI6c]:%u",
+				   &r->key.sip.ip6, ntohs(r->key.sport),
+				   &r->key.dip.ip6, ntohs(r->key.dport));
+		else
+			seq_printf(seq, "%pI4:%u -> %pI4:%u",
+				   &r->key.sip.ip4, ntohs(r->key.sport),
+				   &r->key.dip.ip4, ntohs(r->key.dport));
+		seq_printf(seq, " ipproto:%u\n", r->key.proto);
+	}
+	mutex_unlock(&ft->lock);
+	return 0;
+}
+DEFINE_SHOW_ATTRIBUTE(ppe_ft_requests);
+
 /**
  * ppe_flowtable_debugfs_init - Expose the flowtable state in debugfs
  * @ppe_dev: PPE device
@@ -4302,12 +5077,69 @@
 	ft->debugfs_pipeline = debugfs_create_file("pipeline_state", 0400,
 						   ppe_dev->debugfs_root, ft,
 						   &ppe_ft_pipeline_state_fops);
+	ft->debugfs_vlan_ingress = debugfs_create_file("vlan_ingress", 0400,
+						       ppe_dev->debugfs_root, ft,
+						       &ppe_ft_vlan_ingress_fops);
 	ft->debugfs_reasons = debugfs_create_file("reason_counters", 0400,
 						  ppe_dev->debugfs_root, ft,
 						  &ppe_ft_reason_counters_fops);
 	ft->debugfs_resources = debugfs_create_file("resource_stats", 0400,
 						    ppe_dev->debugfs_root, ft,
 						    &ppe_ft_resource_stats_fops);
+	ft->debugfs_requests = debugfs_create_file("flow_requests", 0400,
+						   ppe_dev->debugfs_root, ft,
+						   &ppe_ft_requests_fops);
+}
+
+/* A bounded control-plane history. No packet-path allocation or printk. */
+static void ppe_ft_request_record(struct ppe_flowtable *ft,
+				  struct flow_cls_offload *f, int result)
+{
+	struct flow_rule *rule = flow_cls_offload_flow_rule(f);
+	struct ppe_ft_request *request;
+	struct ppe_ft_flow *flow;
+
+	mutex_lock(&ft->lock);
+	request = &ft->requests[ft->request_count++ % PPE_FT_REQUEST_HISTORY];
+	memset(request, 0, sizeof(*request));
+	request->cookie = f->cookie;
+	request->result = result;
+	flow = rhashtable_lookup_fast(&ft->flows, &f->cookie, ppe_ft_params);
+	request->index = flow ? flow->index : -1;
+	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_BASIC)) {
+		struct flow_match_basic match;
+
+		flow_rule_match_basic(rule, &match);
+		request->key.proto = match.key->ip_proto;
+	}
+	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_IPV4_ADDRS)) {
+		struct flow_match_ipv4_addrs match;
+
+		flow_rule_match_ipv4_addrs(rule, &match);
+		request->key.sip.ip4 = match.key->src;
+		request->key.dip.ip4 = match.key->dst;
+	} else if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_IPV6_ADDRS)) {
+		struct flow_match_ipv6_addrs match;
+
+		flow_rule_match_ipv6_addrs(rule, &match);
+		request->key.is_ipv6 = true;
+		request->key.sip.ip6 = match.key->src;
+		request->key.dip.ip6 = match.key->dst;
+	}
+	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_PORTS)) {
+		struct flow_match_ports match;
+
+		flow_rule_match_ports(rule, &match);
+		request->key.sport = match.key->src;
+		request->key.dport = match.key->dst;
+	}
+	if (flow_rule_match_key(rule, FLOW_DISSECTOR_KEY_META)) {
+		struct flow_match_meta match;
+
+		flow_rule_match_meta(rule, &match);
+		request->ingress_ifindex = match.key->ingress_ifindex;
+	}
+	mutex_unlock(&ft->lock);
 }
 
 static int ppe_ft_block_cb(enum tc_setup_type type, void *type_data,
@@ -4323,6 +5155,7 @@
 	switch (f->command) {
 	case FLOW_CLS_REPLACE:
 		ret = ppe_ft_flow_replace(ft, f);
+		ppe_ft_request_record(ft, f, ret);
 		atomic_long_inc(&ft->replace_calls);
 		if (ret == -EOPNOTSUPP)
 			atomic_long_inc(&ft->replace_unsupported);
@@ -4537,19 +5370,18 @@
 	for (i = 0; i < PPE_FT_PPPOE_SESSIONS; i++) {
 		if (!ft->pppoe[i].ref.refcnt)
 			continue;
-		if (ft->pppoe[i].hw_active) {
-			ppe_pppoe_session_set(ft->ppe_dev, i, false, 0, NULL, 0);
-			ppe_l3_if_clear(ft->ppe_dev,
-					ppe_ft_pppoe_l3_if(ft, i));
-		}
+		if (ppe_ft_pppoe_deactivate(ft, i))
+			continue;
 		ppe_ft_vlan_put(ft, ft->pppoe[i].vlan);
 		memset(&ft->pppoe[i], 0, sizeof(ft->pppoe[i]));
 	}
 	mutex_unlock(&ft->lock);
 
 	debugfs_remove(ft->debugfs_resources);
+	debugfs_remove(ft->debugfs_requests);
 	debugfs_remove(ft->debugfs_reasons);
 	debugfs_remove(ft->debugfs_pipeline);
+	debugfs_remove(ft->debugfs_vlan_ingress);
 	debugfs_remove(ft->debugfs_rows);
 	debugfs_remove(ft->debugfs_active);
 	debugfs_remove(ft->debugfs);
