From: pbs05 <27010143+pbs05@users.noreply.github.com>
Subject: [PATCH 3/3] arm64: dts: airoha: add EN7581 parallel NAND nodes

Describe the EN7581 parallel NFI and its external ECC engine as separate
devices. Both use the SoC SPI clock and complete transfers by polling.

The NFI register range includes the EN7581 byte counters. Keep both nodes
disabled because the NFI register bank and package pins are shared with
SNFI. Each board selects exactly one flash interface.

Signed-off-by: pbs05 <27010143+pbs05@users.noreply.github.com>

diff --git a/arch/arm64/boot/dts/airoha/en7581.dtsi b/arch/arm64/boot/dts/airoha/en7581.dtsi
index ff6908a76..73ea272aa 100644
--- a/arch/arm64/boot/dts/airoha/en7581.dtsi
+++ b/arch/arm64/boot/dts/airoha/en7581.dtsi
@@ -173,6 +173,35 @@ spi_nand: nand@0 {
 			};
 		};
 
+		pnand: nand-controller@1fa11000 {
+			compatible = "airoha,en7581-nfc";
+			reg = <0x0 0x1fa11000 0x0 0x580>;
+
+			clocks = <&scuclk EN7523_CLK_SPI>;
+			clock-names = "nfi_clk";
+			ecc-engine = <&pnand_ecc>;
+
+			#address-cells = <1>;
+			#size-cells = <0>;
+
+			/*
+			 * Serial and parallel NAND share the NFI register bank and pins.
+			 * Board DTS selects one data path and its corresponding pinmux.
+			 * Both controller nodes stay disabled in the SoC description.
+			 */
+			status = "disabled";
+		};
+
+		pnand_ecc: ecc@1fa12000 {
+			compatible = "airoha,en7581-nand-ecc";
+			reg = <0x0 0x1fa12000 0x0 0x150>;
+
+			clocks = <&scuclk EN7523_CLK_SPI>;
+			clock-names = "nfiecc_clk";
+
+			status = "disabled";
+		};
+
 		scuclk: clock-controller@1fb00000 {
 			compatible = "airoha,en7581-scu";
 			reg = <0x0 0x1fb00000 0x0 0x970>;
